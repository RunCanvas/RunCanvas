# 작업 정리 — 런꾸·워치 연동·기록 저장 (2026-09-08~09)

작성일: 2026-09-09 · 작업 브랜치: `feature/heart-rate`

## 정리 기준과 현재 Git 상태

- 이 대화의 작업 흐름과 현재 미커밋 변경을 대조해 정리했다.
- 기준 커밋은 `7e38bff` — 2026-09-08 09:22 KST, PR #45 머지다. 이 커밋 자체를 이번 개인 작업으로 계산하지 않는다.
- 아래 구현 변경은 문서 작성 시점에 아직 커밋되지 않았다. 이 문서 작성 과정에서도 커밋·푸시·머지를 하지 않았다.
- 미커밋 diff에는 개별 수정 시각이 남지 않는다. 따라서 런꾸·워치 선행 작업을 9월 8일/9일로 억지로 나누지 않고 **이틀간 누적 작업**으로 기록한다. 아이콘 적용, 워치 시작 연동, 50m 저장 기준은 9월 9일 대화에서 확인된다.
- 오래된 대화에 나온 모든 기능을 이번 신규 구현으로 적지는 않았다. 기존 코드에 있던 기능은 아래에서 구분한다.

## 한눈에 보는 변경

| 영역 | 이번에 추가·수정한 내용 |
| --- | --- |
| 런꾸 | 텍스트 수정 진입 개선, 선택 박스 X 삭제, 이미지 스티커 추가, 뱃지 추가 UI 제거, 뒤로가기, 실행 취소, 회전·크기 동시 조절 |
| 워치 화면 | 페이스 표시, 지표별 고대비 색상, 작은 화면에서 수치 가독성 개선 |
| 워치 거리 | 연결이 끊긴 동안 거리 표시 연속성 보완, 폰 최종 거리·시간 전달 |
| 기록 목록 | 전체 기록에서 상세 화면으로 들어가지 못하는 문제 대응 |
| 앱 아이콘 | 검은 러너 + 연두색 밑줄 이미지로 아이폰·워치 아이콘 교체 |
| 워치 시작 | 시작 명령 즉시 전송, 폰 러닝 화면 표시, 계정 연결·위치 권한 안내 보완 |
| 기록 저장 | 시간과 무관하게 50m 이하 기록 제외, 폰·워치·복구·건강 앱 가져오기에 적용 |

## 1. 런꾸 편집 기능 개선

### 텍스트 수정과 삭제

- 텍스트 수정 로직은 기존에도 있었으나, 사용자가 찾기 쉽도록 선택 박스의 연필 버튼에서 수정창을 열 수 있게 연결했다.
- 기존 텍스트를 수정할 때 새 스티커를 만드는 대신 선택한 스티커의 내용을 바꾼다.
- 선택 박스 위 삭제 버튼을 **왼쪽 위 X 표시**로 정리했다.
- 하단 도구 영역의 기존 **휴지통 삭제 버튼은 유지**했다.
- 기존 뱃지 추가 메뉴와 해당 메뉴용 뱃지 조회 코드를 제거했다. 앱 전체의 레벨·뱃지 기능이나 뱃지 모델을 삭제한 것은 아니다.

### 이미지 스티커 추가

- 배경 사진과 별개로 사진을 캔버스 위에 올릴 수 있는 **이미지 추가 버튼**을 넣었다.
- `PhotosPicker`로 사진을 선택하고 `CanvasSticker.Kind.image(UIImage)`로 다룬다.
- 이미지도 위치 이동·크기 조절·회전·삭제 대상이 된다.
- 이미지 비율을 유지해 표시하고, 큰 이미지는 긴 변 1,600 기준으로 축소 준비한다.
- 로딩 표시, 중복 선택 방지, 20초 로딩 제한과 실패 안내를 넣었다.
- 사진 자체의 색을 통째로 바꾸는 기능을 새로 만든 것은 아니다.

### 뒤로가기와 실행 취소

- 상단의 기존 취소 진입을 뒤로가기 형태로 정리했다.
- 별도로 **실행 취소 버튼**을 추가했다. 뒤로가기는 화면 이동이고 실행 취소는 편집 내용 복원이다.
- 배경과 스티커 배열을 편집 전 스냅샷으로 보관한다.
- 추가·삭제·텍스트 수정·배경 변경·이동·크기·회전·스타일 변경 등에 실행 취소를 연결했다.
- 이력은 최대 **30개**이며, 복원하면 선택과 회전 모드를 해제한다.
- 드래그의 매 프레임이 아닌 시작 시점에 이력을 남기도록 했다.
- 다시 실행(Redo)이나 앱 재실행 후 실행 취소 이력 복원은 이번 범위가 아니다.

### 회전 모드와 한 손 조작

- 하단 휴지통 옆에 회전 모드 버튼을 추가했다.
- 일반 모드에서는 선택 박스 오른쪽 아래 손잡이가 크기 조절을 담당한다.
- 회전 모드를 켜면 같은 손잡이가 회전 아이콘으로 바뀐다.
- 손잡이를 드래그하면 **중심과의 거리로 크기**, **중심을 기준으로 한 각도 변화로 회전**을 동시에 바꾼다.
- `atan2`로 짧은 각도 차이를 누적해 ±180° 경계에서 회전이 튀는 현상을 줄였다.
- 스케일 범위는 기존 조작과 같은 `0.3...4`를 사용한다.
- 기존 두 손가락 확대·회전과 정렬 가이드는 유지했다. 정렬선 자체를 이번에 처음 만든 것은 아니다.

관련 파일:

- `RunCanvas/Features/Canvas/CanvasStudioView.swift`
- `RunCanvas/Features/Canvas/StickerCanvas.swift`
- `RunCanvas/Models/Sticker.swift`

## 2. 워치 화면 가독성과 거리 동기화

### 화면

- 거리·시간·심박과 함께 **페이스(PACE)**를 표시한다.
- 현재 페이스 값은 표시 거리와 경과 시간으로 계산한 **평균 페이스**다. 순간 페이스가 아니다.
- 거리 cyan, 시간 white, 페이스 yellow, 심박 pink로 구분하고 보조 문구도 대비를 높였다.
- 작은 워치 화면에서 숫자가 잘리지 않도록 한 줄 표시·축소·고정폭 숫자 처리를 사용했다.
- 테스트 대상은 사용자 Series 5, 동하 SE다. SE 세대와 실제 OS 버전은 별도 확인이 필요하다.

### 교량·터널 등에서 거리 표시가 달라지는 문제 대응

- 함께 기록할 때는 폰 거리 값을 기준으로 사용한다.
- 폰 스냅샷이 끊기면 워치의 전체 누적 거리로 바로 바꾸지 않고, **마지막 폰 거리 + 그 이후 워치 거리 증가분**으로 표시를 이어간다.
- 폰 종료 명령에 최종 거리와 시간을 함께 넣었다.
- 워치 종료 화면과 폰에 보내는 요약은 최종 거리 값으로 고정하고, 폰의 명시적인 종료 수치가 도착하면 우선 반영한다.
- 이는 값이 갑자기 바뀌는 문제와 종료 수치 불일치를 줄이는 처리다. GPS 음영 구간의 실제 경로를 완전히 복구하거나 실거리 정확도를 보장하는 것은 아니다.

관련 파일:

- `RunCanvas Watch App/WatchRunView.swift`
- `RunCanvas Watch App/WatchWorkoutManager.swift`
- `RunCanvas Watch App/WatchConnectivityService.swift`
- `RunCanvas/Services/WatchConnectivityService.swift`
- `RunCanvas/Features/Running/RunCoordinator.swift`

## 3. 전체 기록 → 상세 기록 진입 오류

- 증상: 기록 탭에서 전체 기록을 열고 상세를 보려 할 때 멈추거나 반응하지 않는 것으로 보임.
- 목록 행 뒤에 숨겨진 `NavigationLink`와 빈 라벨을 사용하는 구조를 확인했다.
- 행을 명시적인 `Button`으로 바꾸고, 선택한 기록을 `selectedRun` 상태에 넣어 `navigationDestination(item:)`으로 이동하도록 수정했다.
- 행 전체의 터치 영역과 UI 테스트 식별자를 지정했다. 기존 스와이프 삭제는 유지했다.
- 전체 기록 → 상세 → 뒤로가기를 세 번 반복하는 UI 회귀 테스트를 추가하고 통과를 확인했다.
- 실기기에서 발생한 멈춤을 직접 재현해 단일 원인을 확정한 것은 아니므로, 같은 폰에서 재확인이 필요하다.

관련 파일: `RunCanvas/Features/Records/RunListView.swift`, `RunCanvasUITests/RecordsScreenUITests.swift`

## 4. 앱 아이콘 변경

- 여러 로고 방향을 검토한 뒤, 사용자가 첨부한 **검은 러너 + 형광 연두색 곡선 밑줄**을 임시 앱 아이콘으로 선택했다.
- 원본은 1,254 × 1,254, 투명 배경 이미지였다.
- 디자인은 유지하고 흰 배경을 합성해 1,024 × 1,024의 투명도 없는 PNG로 패키징했다.
- 아이폰과 워치의 `AppIcon.appiconset`에 각각 `AppIcon.png`를 추가하고 `Contents.json`에 연결했다.
- 스플래시 화면을 같은 이미지로 교체한 작업은 아니다. 별도 다크·틴트 전용 원본도 이번에는 추가하지 않았다.

관련 폴더:

- `RunCanvas/Assets.xcassets/AppIcon.appiconset/`
- `RunCanvas Watch App/Assets.xcassets/AppIcon.appiconset/`

## 5. 워치에서 시작했는데 폰이 반응하지 않는 문제

### 확인한 구조

- 시작 명령이 `transferUserInfo` 대기열로만 전달돼 즉시 반영을 기대하기 어려웠다.
- 명령을 받은 뒤 폰 러닝 화면을 자동 표시하는 연결이 없었다.
- 계정 ID가 주로 러닝 화면에 들어갈 때 연결되어, 그 화면에 진입하지 않은 상태의 시작에도 보완이 필요했다.

### 변경

- 폰·워치 모두 명령을 대기열에 남기되, 연결 가능 상태에서는 같은 메시지를 `sendMessage`로도 즉시 보낸다.
- 같은 메시지의 재도착은 기존 타임스탬프 검사로 중복 처리하지 않는다.
- 메인 탭에서 로그인 계정을 코디네이터에 연결한다.
- 워치 시작을 폰이 정상 수락하면 `showsWatchRun`으로 러닝 화면을 표시한다.
- 위치 권한 때문에 시작하지 못하면 폰에 안내한다.
- 러닝 화면을 닫는 버튼과 러닝 종료는 구분된다. 화면만 닫아도 러닝 자체를 종료하는 것은 아니다.
- 잠긴 폰을 강제로 잠금 해제하거나 앱을 전면으로 띄우는 기능은 아니다. 먼저 폰 앱을 열고 로그인·위치 권한을 갖춘 상태에서 실기기 검증해야 한다.

관련 파일: 양쪽 `WatchConnectivityService.swift`, `RunCoordinator.swift`, `RootTabView.swift`, `WatchConnectivityServiceTests.swift`

## 6. 불필요한 짧은 기록 저장 방지 — 최종 확정 기준

**시간과 관계없이 누적 이동 거리 50m 이하는 저장하지 않는다.**

| 종료 시 조건 | 처리 |
| --- | --- |
| 30초·0m | 저장 안 함 |
| 1분·0m | 저장 안 함 |
| 10분·0m | 저장 안 함 |
| 시간 무관·정확히 50m | 저장 안 함 |
| 1분 미만이어도 50m 초과 | 저장 대상 |

- 최초에 제안했던 ‘1분 이내이면서 50m 이하’ 조건은 최종 기준이 아니다.
- 시작 후 1분이 되면 러닝을 자동 중단하는 타이머를 추가한 것도 아니다. **종료/저장 시점**에 판정한다.
- NaN·무한대 같은 비정상 거리도 저장 대상으로 인정하지 않는다.
- 기존 저장 기록을 일괄 삭제하지 않았다.

### 적용한 경로

- 폰 종료: SwiftData 삽입과 HealthKit 저장 전에 검사한다. 제외할 때 심박 스트림·체크포인트를 정리하고 `finished` 상태로 끝낸다.
- 코디네이터: ‘저장 없이 정상 종료’를 로그인 오류로 오인하지 않도록 처리한다. 결과 기록 대신 제외 안내를 표시한다.
- 일반 러닝·트레이닝 화면: “이동 거리가 50m 이하인 러닝은 저장하지 않아요” 안내를 추가했다.
- 워치 종료: 기준 이하이면 `discardWorkout()`을 사용하고 건강 앱 저장·완료 요약 전송을 건너뛴다. 워치 종료 문구도 저장되지 않았다고 표시한다.
- 중단 복구: 50m 이하 체크포인트는 기록으로 다시 만들지 않는다.
- 건강 앱/워치 요약 가져오기: 같은 거리 기준을 적용한다. 기존의 ‘60초 이상·100m 이상’ 조건을 새 기준으로 바꿨다.
- 폰과 워치에 각각 둔 `RunSavePolicy`가 동일한지 테스트한다.

관련 파일:

- `RunCanvas/Models/RunSavePolicy.swift` — 신규
- `RunCanvas Watch App/RunSavePolicy.swift` — 신규
- `RunCanvas/Features/Running/RunSession.swift`, `RunCoordinator.swift`, `RunView.swift`
- `RunCanvas/Features/Training/TrainingRunView.swift`
- `RunCanvas/Services/HealthImport.swift`
- `RunCanvas Watch App/WatchWorkoutManager.swift`, `WatchRunView.swift`

## 7. 검증 결과와 테스트 변경

- [x] 최종 iOS 단위 테스트 **168개 통과 / 실패 0 / 건너뜀 0**.
  - 결과: `Test-RunCanvas-2026.09.09_17-20-38-+0900.xcresult`
  - 환경: iPhone 17 Pro 시뮬레이터, iOS 26.5.
- [x] 워치 시작 연동 변경 당시 통신·코디네이터 관련 테스트 9개 통과. 최종 전체 테스트에도 포함된다.
- [x] 전체 기록 → 상세 → 복귀 3회 반복 UI 테스트 통과. 이는 해당 수정 당시의 결과이며, 최종 변경 후 UI 테스트 전체를 다시 실행한 것은 아니다.
- [x] 최종 `RunCanvas Watch App` 스킴의 watchOS Simulator 빌드 통과.
- [x] 이미지 규격 1,024 × 1,024, alpha 없음 확인.
- [x] 코드 변경 시 `git diff --check` 통과.

테스트 파일:

- `RunCanvasTests/RunSavePolicyTests.swift` — 신규: 경계값, 폰·워치 정책 일치, 장시간 무이동 종료, 복구 제외.
- `RunCanvasTests/WatchConnectivityServiceTests.swift` — 최종 수치 메시지, 중복 시작, 종료 후 지연 시작 방지.
- `RunCanvasTests/RunSessionTests.swift` — 저장 테스트에 실제 이동 데이터를 넣어 새 기준에 맞춤.
- `RunCanvasTests/HealthImportTests.swift` — 50m 이하 제외, 1분 미만 50m 초과 허용.
- `RunCanvasTests/RunCoordinatorTests.swift` — 폰 무이동 종료의 저장 제외 기대값 반영.
- `RunCanvasTests/VoiceCoachTests.swift` — 정상 종료 음성 테스트에 유효 거리 추가.
- `RunCanvasUITests/RecordsScreenUITests.swift` — 상세 진입 반복 회귀 테스트.

중간에 새 안내창 추가로 `RunView`의 SwiftUI 타입 검사 시간이 초과됐다. 화면 본체를 `runningContent`로 분리한 뒤 재빌드·테스트를 통과했다. 기존 `VoiceCoach` 등 Sendable 관련 경고는 남아 있으며 이번 작업에서 모두 해결한 것은 아니다.

## 8. 실기기에서 이어서 확인할 것

- [ ] 아이폰과 워치 **둘 다 최신 코드로 재설치**하고 아이콘 확인.
- [ ] 폰 홈·기록·런꾸 탭 각각에서 워치로 시작 → 폰 러닝 화면과 시간 진행 확인.
- [ ] 폰 화면 닫기와 러닝 종료가 구분되는지 확인.
- [ ] 위치 권한이 없는 폰에서 시작 실패 안내 확인.
- [ ] 양쪽 일시정지·재개·종료 및 두 번째 러닝 확인.
- [ ] Series 5·SE에서 거리/시간/페이스/심박 가독성 확인.
- [ ] 교량·터널·연결 끊김 후 거리 표시와 최종 기록 비교.
- [ ] 양쪽 GPS 값이 50m 경계에서 서로 다를 때 저장 여부와 지연 동기화 확인. 실기기 왕복 통신의 타이밍까지 단위 테스트로 보장한 것은 아니다.
- [ ] 0m·50m 이하 종료 시 기록/건강 앱에 남지 않는지, 50m 초과 기록은 남는지 확인.
- [ ] 종료 후 앱 재실행·건강 앱 가져오기 시 짧은 기록 재등장 여부 확인.
- [ ] 런꾸 텍스트 수정, X/휴지통 삭제, 이미지 추가, 연속 회전·확대, 실행 취소 확인.
- [ ] 실제 기록이 많은 폰에서 전체 기록 → 상세 반복 진입 확인.

## 9. 나중에 커밋할 때 추천하는 묶음

아래는 **추천안**이며 실제 커밋을 만든 것은 아니다. 한 파일에 여러 기능이 섞였으므로 파일 전체를 무조건 묶기보다는 변경 구간별로 나누거나 관련 작업을 하나로 묶는 편이 안전하다.

1. `feat: 런꾸 이미지 추가와 실행 취소 및 회전 편집 개선`
   - CanvasStudioView, StickerCanvas, Sticker 모델.
2. `fix: 전체 기록에서 상세 화면 진입 안정화`
   - RunListView와 대응 UI 테스트.
3. `feat: 아이폰 및 워치 앱 아이콘 적용`
   - 양쪽 AppIcon 이미지와 Contents.json.
4. `fix: 워치 러닝 시작 연동과 거리 표시 개선`
   - 워치 화면/매니저, 양쪽 통신, RootTabView, 코디네이터와 테스트의 해당 구간.
5. `fix: 50m 이하 러닝 저장 및 재가져오기 방지`
   - 저장 정책, 폰/워치 종료, 가져오기, 안내 화면, 관련 테스트.
6. `docs: 9월 8~9일 작업 내용과 검증 결과 정리`
   - 이 문서.

특히 4번과 5번은 `WatchWorkoutManager`, `WatchRunView`, `RunCoordinator`가 겹친다. 나눌 경우 각 커밋 상태에서도 빌드되는지 확인해야 한다. 지금의 미커밋 변경이 사라진 것은 아니므로, 날짜별 커밋을 억지로 복원하기보다 기능별 설명을 정확히 남기는 것이 우선이다.
