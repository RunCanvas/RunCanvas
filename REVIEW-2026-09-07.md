# 전체 리뷰 — 발견과 처리 결과 (2026-09-07)

18개 리뷰어(영역 8 × 렌즈 2 + 테마 + 보안)가 9,153줄을 읽고 **161건**을 냈다.
아래 "심각도" 세 절은 **처음 받은 발견 그대로의 기록**이다(체크박스 없음 — 상태는 여기서 읽지 말 것).
실제 처리 상태는 문서 뒤쪽 순서대로 읽는다: `2차 확인` → `3차 처리 결과` → `4차 남은 것 처리`.

- 처리 요약: 2차에서 이미 닫힘 106 · 문제 아님 15 · 그 자리에서 고침 11 · 남음 26
  → 3차에서 21개 항목을 고치고 5개는 이유를 적고 남김 → 4차에서 리뷰 중 끊겼던 작업을 마저 처리
- 규칙: 테마(시스템 배경 + 회색 카드 + 16 라운드 + 20 여백, 흑백 틴트, 레벨 컬러는 주요 버튼만)는 바꾸지 않는다.
- 표시(2차 절): `[x]` 고침 · `[-]` 안 함(이유는 3차 절에)

## 심각도 높음 (11건)

### `RunCanvas/Features/Marathon/MarathonSchedule.swift`

- **날짜를 Asia/Seoul 자정으로 파싱해 놓고 표시·묶기는 Calendar.current(기기 시간대·달력)로 함** — 버그, 200줄
  - 무엇이: parseDate 는 'yyyy-MM-dd' 를 Asia/Seoul 00:00 으로 만든다(L230-236). 그런데 sections()·daysAway() 기본값과 뷰의 dateText(L274)·footerText(L305)·monthHeader(L68) 는 Calendar.current 를 쓴다. 기기 시간대가 서울보다 서쪽이면(해외 체류자, 미국에서 심사하는 App Store 리뷰어, 시간대가 다른 시뮬레이터) 2026-10-03 KST 는 10-02 로 찍혀 '10월 2일 (금)' 이 되고, 11-01 대회는 '2026년 10월' 섹션에 들어가며, 대회 당일이 하루 일찍 목록에서 사라진다. 기기 달력이 불교력/일본력이면 헤더가 '2569년 10월' 로 나온다. 앱 규칙(날짜·요일을 기기 로케일에 맡기지 않는다)을 언어만 지키고 시간대·달력은 안 지킨 상태. 유닛 테스트는 서울 캘린더를 명시로 넘겨서 잡히지 않는다.
  - 어떻게: MarathonSchedule 에 `static let calendar: Calendar = { var c = Calendar(identifier: .gregorian); c.timeZone = TimeZone(identifier: "Asia/Seoul") ?? .current; return c }()` 하나 두고, sections()/daysAway() 의 기본 인자와 MarathonScheduleView·MarathonCard 의 Calendar.current 네 곳을 전부 이걸로 바꾼다. 테스트 하나는 calendar 인자 없이(기본값) 돌려 기본값이 서울인지 고정.

### `RunCanvas/Features/Onboarding/SplashView.swift`

- **스플래시가 다크 모드에서 흰 화면으로 번쩍이고, 로그인할 때마다 반복됨** — UI/UX, 14줄
  - 무엇이: `Color.white`(14) + `.foregroundStyle(.black)`(20) + `.gray`(27)로 색이 고정돼 있어 다크 모드에서는 전체 화면이 흰색으로 떴다가 검정 시스템 배경의 로그인/홈으로 바뀐다. 게다가 AppRouter는 `.task(id: auth.userID)`로 로그인·로그아웃 때마다 load()를 다시 돌리고 84행에서 `isLoading = true`로 되돌리므로, 로그인 성공 뒤 프로필을 가져오는 동안 이 흰 스플래시가 매번 다시 나타난다. 테마 규칙(시스템 배경 + 흑백)에도 어긋난다.
  - 어떻게: SplashView에서 `Color.white` → `Color(.systemBackground)`, `.black` → `.primary`, `.gray` → `.secondary`로 바꾼다.

### `RunCanvas/Features/Records/RunListView.swift`

- **기록 삭제가 스와이프 한 번에 확인 없이 끝난다(코스 삭제는 확인을 받음)** — UI/UX, 25줄
  - 무엇이: `.onDelete`는 풀 스와이프 한 번으로 로컬 기록 + 꾸민 이미지(CanvasStorage) + 서버 사본까지 지우고 되돌릴 수 없다. 같은 앱에서 CourseDetailView(줄 67~77)는 코스 하나를 지울 때도 confirmationDialog로 확인을 받는다. 남의 눈에 안 보이는 코스보다 몇 달치 러닝 기록이 훨씬 되돌리기 어려운데, 더 가벼운 흐름으로 지워진다. 목록 스크롤 중 실수 스와이프가 잦은 제스처라 확인 한 단계가 필요하다.
  - 어떻게: `.onDelete` 대신 행에 `.swipeActions(edge: .trailing) { Button("삭제", role: .destructive) { pendingDelete = run } }`를 달고, CourseDetailView와 같은 `confirmationDialog("이 기록을 삭제할까요?", titleVisibility: .visible)`(메시지: '서버 사본과 꾸민 이미지도 함께 지워지고 되돌릴 수 없어요')에서 기존 삭제 코드를 실행한다.

### `RunCanvas/Features/Running/RunCoordinator.swift`

- **러닝 한 번 끝나면 같은 프로세스에서 두 번째 러닝(폰·워치 .start 모두) 시작 불가** — 버그, 99줄
  - 무엇이: RunSession.state는 idle→running→paused→finished로만 가고 finished→idle로 돌아오는 코드가 없다(RunSession.swift 전체에 `state = .idle` 없음). RunSession.start()(RunSession.swift:73)는 `guard state == .idle`이고, RunCoordinator.session은 `let`이며 RunCanvasApp에서 앱 수명 @State로 한 번만 만든다. 따라서 첫 러닝을 finish한 뒤에는 RunCoordinator.handle(.start)(:99 `guard session.state == .idle`)가 워치의 .start를 전부 버리고, RunView의 버튼도 `.finished: break`라 아무 일도 안 한다. 워치에서 시작한 두 번째 러닝은 폰에 기록되지 않고 워치 단독으로만 남는다. 앱을 완전히 죽였다 켜야 풀린다. finish()가 accumulated·heartRateSamples를 초기화하지 않아 guard만 풀면 시간·심박이 이전 러닝에 누적된다.
  - 어떻게: RunSession.start(): `guard state == .idle || state == .finished`로 바꾸고 `accumulated = 0; heartRateSamples = []; heartRate = nil`을 리셋한다. RunCoordinator.handle(.start)와 RunView(.task의 `session.state == .idle`, 버튼 switch의 `.finished: break`)도 .finished를 idle과 같이 취급한다. RunSessionTests에 start→finish→start 후 state == .running, elapsedSeconds == 0 검증 1개 추가.

### `RunCanvas/Features/Running/RunView.swift`

- **'러닝 종료'가 확인 없이 한 번에 끝나고, 탭 영역이 글자 높이(약 20pt)뿐** — UI/UX, 135줄
  - 무엇이: `Button("러닝 종료")`는 탭 즉시 `runs.finish`를 불러 세션을 닫고 저장·결과 화면까지 간다. 되돌릴 방법이 없다(재개 불가). 이 버튼은 달리는 동안 계속 노출되고 일시정지 버튼 바로 아래 16pt 간격에 있어, 흔들리는 손으로 일시정지를 누르려다 종료하기 쉽다. `.padding(.top, 16)`은 버튼 바깥에 붙어 있어 히트 영역을 넓히지 못하고, subheadline 텍스트 그대로 약 60×20pt다(44pt 기준 미달). 코스 삭제(CourseDetailView)는 confirmationDialog로 확인을 받는데, 그보다 훨씬 되돌리기 어려운 러닝 종료에는 확인이 없다.
  - 어떻게: RunView:135를 `@State private var confirmsFinish = false` + `.confirmationDialog("러닝을 종료할까요?", isPresented: $confirmsFinish, titleVisibility: .visible) { Button("종료", role: .destructive) { finish() }; Button("계속 달리기", role: .cancel) {} }`로 바꾸거나, 더 단순하게 종료 버튼을 `session.state == .paused`일 때만 보여준다(일시정지 → 종료 두 단계). 버튼 라벨에 `.frame(minHeight: 44).contentShape(Rectangle())`을 넣어 탭 영역을 확보한다. RunFlowUITests:50-52는 확인 버튼(또는 일시정지 선행) 탭으로 갱신.
- **첫 러닝: 위치 권한 프롬프트 동안 타이머가 돌고, 거부해도 러닝이 이어져 0.00km가 저장됨** — UI/UX, 229줄
  - 무엇이: `startRun()`은 `.denied/.restricted`만 막고, 첫 실행의 `.notDetermined`는 `default`로 흘러 `runs.start()` → `location.requestPermission()`과 타이머 시작이 동시에 일어난다. 사용자가 시스템 프롬프트를 읽는 5~10초가 첫 기록 시간에 그대로 들어간다. 프롬프트에서 '허용 안 함'을 고르면 `locationAuthorization`이 `.denied`로 바뀌지만 아무도 관찰하지 않아 러닝은 0.00km로 계속 돌고, 종료하면 0km 기록이 저장된다 — 232행 주석의 의도("0.00km를 저장하게 두지 않기 위해")와 어긋난다. 같은 `.task`가 곧바로 `requestHealthAuthorization()`도 불러 첫 러닝에 위치 알럿과 건강 시트가 겹쳐 뜬다. `RunSession.requestLocationPermission()`(60행)은 이미 있는데 호출처가 없다.
  - 어떻게: RunView.startRun()에 `case .notDetermined: session.requestLocationPermission()` 분기를 추가하고, `.onChange(of: session.locationAuthorization) { _, status in switch status { case .authorizedWhenInUse, .authorizedAlways: if session.state == .idle && startImmediately { runs.start() } case .denied, .restricted: showsLocationDenied = true default: break } }`를 단다. 건강 권한 요청은 위치가 결정된 뒤(같은 onChange 안이나 시작 직후)로 미뤄 프롬프트가 겹치지 않게 한다. 러닝 중 거부됐을 때의 알럿 문구(197행)는 '다시 시작해 주세요' 대신 '종료하면 거리 없이 저장돼요'처럼 지금 상태에 맞게 나눈다.

### `RunCanvas/Features/Settings/SettingsView.swift`

- **아무 효과 없는 설정 항목 3개(알림·거리 단위·1회 목표 거리)** — UI/UX, 182줄
  - 무엇이: `isNotificationEnabled`(23)와 `distanceUnit`(24)은 SettingsView 밖 어디서도 읽지 않는다(전체 grep 0건, UNUserNotificationCenter 사용 코드도 없음). 사용자가 "마일 (mi)"을 골라도 앱 전체가 km 그대로고, "알림" 토글은 아무것도 바꾸지 않는다. `targetDistance`(15, "1회 목표 거리" 121행)도 마찬가지로 다른 화면이 읽지 않으며, 푸터(133) "목표는 홈과 기록 화면의 진행률에 쓰여요"는 사실과 다르다 — 홈은 목표를 쓰지 않고 기록(RunStatsView.swift:33,164)은 주간 목표만 쓴다. 동작하지 않는 컨트롤은 사용자가 설정이 고장났다고 느끼게 한다.
  - 어떻게: appSection(182-190)과 "1회 목표 거리" 행(121), 관련 @AppStorage/@State/onChange(15, 23-24, 26, 61, 66-68)를 지운다(기능이 생길 때 다시 추가). 러닝 설정 푸터(133)를 "주간 목표는 기록 화면의 진행률에 쓰여요. 음성 안내는 …"로 고친다.

### `RunCanvas/Models/TrainingProgram.swift`

- **내장 프로그램 세션 id가 실행마다 새로 생겨 완료 진행도가 앱 재시작마다 사라짐** — 버그, 29줄
  - 무엇이: TrainingSession.id는 `var id = UUID()` 기본값이고, builtIn(couchTo5K·thirtyMinutes·intervals)은 `static let`으로 프로세스가 뜰 때 만들어진다. 즉 세션 UUID는 앱을 지울 때가 아니라 **앱을 켤 때마다** 바뀐다(TrainingStore.swift:54 주석 '앱을 지우면'은 틀렸다). TrainingStore.markCompleted는 그 UUID를 저장하므로, 8주 5K를 3세션 하고 앱을 껐다 켜면 0/24로 돌아가고 '이어서 하기'가 항상 1주차 1일을 가리킨다. 테스트(testCompletedSessionsAreCountedPerProgram)는 같은 프로세스 안이라 못 잡는다. 내장 프로그램이 트레이닝 기능의 본체라 사실상 진행도 저장이 안 되는 셈이다.
  - 어떻게: 내장 프로그램·세션 id를 결정적으로 만든다. 예: TrainingProgram(id: UUID(uuidString: "00000000-0000-0000-0001-000000000000")!, …), 세션은 `UUID(uuidString: String(format: "00000000-0000-0000-000%d-%012d", programIndex, sessionIndex))!`(couchTo5K L74, thirtyMinutes L90, intervals L110 세 곳). 또는 완료 키를 UUID 대신 "\(program.name)|\(session.title)" 문자열로 저장한다. 어느 쪽이든 '재시작 후에도 completedCount가 유지된다'는 테스트를 하나 추가(새 UserDefaults 스위트에 저장 → TrainingProgram.builtIn을 다시 참조해도 count 유지).

### `RunCanvas/Services/AuthService.swift`

- **Google·카카오 계정 연결이 절대 완료되지 않음 (콜백 URL을 받는 곳이 없다)** — 버그, 88줄
  - 무엇이: supabase-swift 2.55.1의 `linkIdentity(provider:scopes:redirectTo:)`는 ASWebAuthenticationSession을 쓰지 않는다 — `user/identities/authorize` URL을 `UIApplication.open`으로 Safari에 띄우고 곧바로 리턴한다(AuthClient.swift:1405-1417). 그래서 `link()`는 사용자가 동의 화면을 보기도 전에 `refreshIdentities()`를 돌리고, SettingsView.link()의 `isLinking`도 즉시 false가 되며 `ASWebAuthenticationSessionError` catch는 죽은 코드다. Safari가 `runcanvas://auth-callback?code=…`로 앱을 다시 열어도 앱 어디에도 `.onOpenURL`/`supabase.auth.handle(_:)`가 없어(RunCanvas 전체 grep 결과 0건) PKCE 코드 교환이 일어나지 않는다 → identities는 영원히 그대로, 사용자는 Safari에 남겨진다. 결과적으로 설정의 '연결된 계정' 기능 중 Apple(id_token 경로)만 동작하고 Google·카카오는 항상 실패한다.
  - 어떻게: 최소 수정: AppRouter body에 `.onOpenURL { supabase.auth.handle($0) }` 추가. `handle`→`session(from:)`이 코드를 교환하고 `.signedIn`을 emit하므로 AuthService의 authStateChanges 루프가 새 identities를 받는다(`link()` 뒤의 `refreshIdentities()`는 그대로 둬도 무해). 앱 안에서 끝내고 취소 catch까지 살리려면: `getLinkIdentityURL`로 URL을 받아 LoginView처럼 ASWebAuthenticationSession(callbackURLScheme: "runcanvas")을 직접 띄우고 돌아온 URL을 `try await supabase.auth.session(from:)`에 넘긴다.

### `RunCanvas/Services/CourseService.swift`

- **로딩 중 필터를 바꾸면 요청이 버려져 알약과 목록이 어긋난다** — 버그, 33줄
  - 무엇이: `guard !isLoading else { return }`는 진행 중인 load가 있으면 새 요청을 조용히 버린다. CourseListView는 `.task`(첫 로드)와 `.onChange(of: region/sort/mineOnly)`가 각각 reload()를 던지므로, 첫 로드가 도는 동안(느린 망에서 수 초) 사용자가 지역을 '부산'으로 바꾸면 그 load는 무시되고 '전체 지역' 결과가 도착해 목록에 깔린다. 알약은 '부산', 목록은 서울 코스 — 사용자는 필터가 고장났다고 느끼고, 같은 지역을 다시 눌러도 onChange가 안 울려 복구 방법이 없다. pull-to-refresh도 같은 이유로 스피너만 돌다 즉시 끝난다. (MarathonService의 같은 guard는 서버 필터가 없어 무해하다.)
  - 어떻게: guard를 지우고 세대 토큰으로 마지막 요청만 반영: `private var generation = 0`; load 시작에 `generation += 1; let mine = generation`, 응답 후 `guard mine == generation else { return }` 다음에 `courses`/`failureNotice`/`isLoading = false`를 쓴다. loadRegionCounts도 같은 토큰을 쓰면 개수 배지도 어긋나지 않는다.

### `RunCanvas/Services/WatchConnectivityService.swift`

- **오래 큐에 쌓인 start 명령이 몇 시간 뒤 재생돼 폰이 0km 러닝을 만들고 '러닝 시작'을 말한다** — 버그, 89줄
  - 무엇이: 명령은 transferUserInfo라 상대가 안 닿아도 큐에 남았다가 앱이 다음에 켜질 때 순서대로 배달된다. 워치만 차고 나가서 달리고 워치에서 끝내면 .start와 .end가 큐에 쌓이고, 집에 와서 폰 앱을 열면 RunCoordinator.handle(.start)가 session.start()를 호출해 GPS를 켜고 VoiceCue.start('러닝 시작')를 말한 직후 .end가 도착해 RunSession.finish()가 distance 0·0초 Run을 그대로 insert한다(RunSession.swift 112행에 최소 거리 guard 없음). 기록 목록에 0.00km 쓰레기 기록이 남는다. 반대로 워치 쪽 receive(워치 WatchConnectivityService.swift 81행)도 같아서, 폰에서 시작한 명령이 몇 시간 뒤 워치 앱을 열 때 갑자기 워크아웃을 시작한다. 타임스탬프는 이미 메시지에 실려 있는데 '최신인지'만 보고 '얼마나 오래됐는지'는 안 본다. 참고: 이걸 막으면 워치 단독 러닝은 RunCanvas에 아예 안 남으므로 워치 상태 문구 'Watch 단독 기록'도 '건강 앱에만 저장' 같이 솔직하게 바꿔야 한다.
  - 어떻게: 양쪽 receive()의 command 분기에서 .start에 한해 guard Date().timeIntervalSince1970 - timestamp < 120 else { return } (상수 하나로 뽑아 ponytail: 주석). .pause/.resume/.end는 sessionID guard가 걸러주므로 그대로 둔다. 워치 WatchRunView 13행 문구를 'iPhone 연결 안 됨 · 건강 앱에만 저장'으로.

## 심각도 보통 (32건)

### `.github/workflows/sync-marathons.yml`

- **workflow_dispatch 를 다른 브랜치에서 실행하면 그 브랜치 HEAD 가 develop 으로 push 됨** — 버그, 51줄
  - 무엇이: `git push origin HEAD:develop` 은 체크아웃된 브랜치와 무관하게 develop 을 겨냥한다. 수동 실행(workflow_dispatch) 은 브랜치를 고를 수 있으므로 feature 브랜치나 main 에서 실행하면 봇 커밋뿐 아니라 그 브랜치의 모든 커밋이 develop 으로 올라간다 — develop 이 조상이면 fast-forward 로 조용히 병합되고, 아니면 job 만 빨갛게 실패한다. 또 체크아웃과 push 사이에 누가 develop 에 push 하면 non-fast-forward 로 실패해 동기화가 성공했는데도 실행이 실패로 남는다.
  - 어떻게: `git push origin HEAD:"$GITHUB_REF_NAME"` (또는 그냥 `git push`) 로 체크아웃한 브랜치에만 쓴다. 커밋 뒤 `git pull --rebase origin "$GITHUB_REF_NAME"` 을 넣어 동시 push 를 흡수한다. 스케줄 실행은 기본 브랜치(develop)에서 돌므로 동작이 바뀌지 않는다.

### `RunCanvas/App/AppRouter.swift`

- **첫 프로필 설정 직후 오프라인으로 다시 실행하면 프로필 설정 화면에 갇힘** — 버그, 20줄
  - 무엇이: `profiledUserKey`는 load()가 서버에서 프로필을 확인했을 때만 기록된다(93행). ProfileSetupView 저장 성공 시 onDone은 `hasProfile = true`만 하고 키를 남기지 않는다. 그래서 신규 사용자가 프로필을 만들고 앱을 종료한 뒤 오프라인에서 다시 열면 fetchMine이 throw → 98행에서 키 불일치 → hasProfile=false → 빈 프로필 설정 화면이 또 뜬다. 여기서 시작하기를 눌러도 upsert가 실패해 "저장하지 못했어요"만 반복되는 막다른 화면이다(로그인으로 돌아가는 것 말고는 진행 불가). 95-96행 주석이 막으려던 바로 그 상황이 신규 사용자에게 그대로 남아 있다.
  - 어떻게: AppRouter의 onDone 클로저에서 `UserDefaults.standard.set(auth.userID?.uuidString ?? "", forKey: Self.profiledUserKey)`도 함께 쓴다(93행과 같은 기록을 작은 private func markProfiled(_ id: UUID?)로 묶어 두 곳에서 호출).

### `RunCanvas/Components/ChipRow.swift`

- **칩·알약·지도 핀·텍스트 버튼의 터치 영역이 44pt에 못 미친다** — UI/UX, 15줄
  - 무엇이: ChipRow(padding 6 + subheadline ≈ 27pt), FilterMenuPill(:43-46, 같은 치수), CourseMapBrowseView 핀(:64-70, 11pt 아이콘 + 6pt 패딩 ≈ 23pt, 선택 안 된 핀이 서로 붙어 있을 때 특히 어렵다), 그리고 텍스트만 있는 버튼들 — CourseDetailView:55 '이 코스 삭제', TrainingProgramDetailView:57-58 '편집'/'삭제', TrainingProgramListView:17 '새로 만들기'(subheadline ≈ 20pt) — 모두 히트 영역이 라벨 크기 그대로다.
  - 어떻게: 시각은 그대로 두고 히트 영역만 키운다: 칩·알약·텍스트 버튼은 Button 라벨에 `.frame(minHeight: 44).contentShape(Rectangle())`, 핀은 `.frame(width: 44, height: 44).contentShape(Circle())`로 감싼다.

### `RunCanvas/Features/Auth/LoginView.swift`

- **로그인 진행 중 버튼이 비활성인데 겉모습이 그대로라 탭이 조용히 무시됨** — UI/UX, 46줄
  - 무엇이: `.disabled(isBusy)`만 걸려 있다. PrimaryButton은 배경·글자색을 직접 칠하므로 disabled여도 흐려지지 않는다 — 그래서 ProfileSetupView.swift:92-93과 ProfileEditView.swift:64-65는 `.disabled` 뒤에 수동으로 `.opacity(0.3)`을 덧붙이고 있다. 로그인 화면엔 그게 없어서 OAuth 시트가 닫히고 토큰 교환이 도는 동안(및 Apple 시트 대기 중) 버튼이 멀쩡해 보이지만 눌러도 반응이 없다. 스피너도 없어 사용자는 실패한 줄 안다.
  - 어떻게: 근본 위치인 Components/PrimaryButton.swift에 `@Environment(\.isEnabled)`를 읽어 `.opacity(isEnabled ? 1 : 0.3)`을 넣고, ProfileSetupView(93)·ProfileEditView(65)의 수동 `.opacity`는 지운다. LoginView에는 isBusy일 때 버튼 아래 `ProgressView()` 한 줄을 추가한다.

### `RunCanvas/Features/Auth/ProfileSetupView.swift`

- **체중·키 상한 검증이 없어 비정상 값이 저장되고 기록 상세·스티커 화면까지 크래시로 번짐** — 버그, 18줄
  - 무엇이: ProfileSetupView(line 18-19)와 ProfileEditView(line 25, 129-130)는 `> 0`만 검사한다. 예: 체중 99999999999999999999 → UserDefaults `userWeight` → RunCoordinator.weightKg(RunCoordinator.swift:35-38) → RunMath.calories = 1e20×km → Run.calories에 저장 → RunDetailView.swift:43 `Int(run.calories.rounded())`, StickerCanvas.swift:312 같은 식이 Int 범위 초과로 트랩. 즉 잘못 입력한 한 번의 프로필이 이후 모든 러닝 기록 화면을 크래시시키며, 서버에도 그대로 upsert된다. `Double("inf")`를 붙여넣으면 JSONEncoder가 실패해 '네트워크를 확인하고'라는 엉뚱한 안내가 뜬다.
  - 어떻게: 두 화면의 `height`/`weight` 계산에 상한을 넣는다(예: `(0...300).contains(h)`, `(0...500).contains(w)` — isFinite도 자동 보장). SettingsView의 `isValidTarget` 패턴을 그대로 따라 static 헬퍼 하나로 두고 Setup·Edit이 공유하면 된다.

### `RunCanvas/Features/Canvas/CanvasHomeView.swift`

- **런꾸를 덮어쓴 뒤 갤러리 썸네일이 예전 이미지 그대로** — 버그, 75줄
  - 무엇이: `.task(id: decorated.map(\.id))`는 '저장된 기록의 id 집합'이 바뀔 때만 다시 돌고, `loadThumbnails()`(114-122)는 `thumbnails[run.id] == nil`인 것만 디스크에서 읽는다. 그런데 같은 기록을 다시 꾸며 '앱에 저장 → 덮어쓰기'(CanvasExportSheet:38, 57-62)를 하면 파일명이 `runID.jpg`로 동일해 id 집합도, 파일명도 안 바뀐다 → 편집기를 닫고 돌아온 갤러리는 캐시된 옛 썸네일을 보여준다. 사용자는 방금 저장이 안 된 줄 안다(앱을 완전히 재시작해야 새 이미지). RunDetailView:96의 `.task(id: run.decoratedImageFilename)`도 같은 이유로 두 번째 저장부터 옛 이미지를 보여주는 동일 문제(파일 범위 밖이지만 원인이 같음).
  - 어떻게: CanvasHomeView.swift 두 `fullScreenCover`에 `onDismiss: { thumbnails = [:]; Task { await loadThumbnails() } }`를 붙여 편집기를 닫을 때마다 다시 읽는다(`UIImage(contentsOfFile:)`는 캐시가 없어 새 파일이 바로 반영됨). `isCreating` 경로도 시트에서 이미 꾸민 기록을 고를 수 있으니 둘 다 필요.

### `RunCanvas/Features/Canvas/StickerCanvas.swift`

- **날짜 스티커·홈 최근 러닝 날짜가 기기 로케일을 따름 (한국어 고정 규칙 위반)** — UI/UX, 309줄
  - 무엇이: `run.startedAt.formatted(.dateTime.year().month(.wide).day())`는 기기 언어를 따르므로 영어로 쓰는 기기에서는 내보낸 이미지에 "September 6, 2026"이 박히고, 같은 기록을 한국어 기기에서 저장하면 "2026년 9월 6일"이 된다 — 공유용 결과물이 기기마다 달라진다. RunnerHomeView.swift:147의 `formatted(.dateTime.month().day())`도 같은 문제("Sep 6" vs "9월 6일"). 테마 규칙 "날짜·요일은 기기 로케일에 맡기지 않고 한국어로 고정"에 정면으로 어긋난다. (범위 밖이지만 RunDetailView:47, RunStatsView:151·231, BadgesView:268도 같은 패턴 — 공용 헬퍼가 없어 각자 로케일 없이 부른다.)
  - 어떻게: StickerCanvas.swift:309 와 RunnerHomeView.swift:147의 포맷 스타일에 `.locale(Locale(identifier: "ko_KR"))`를 붙인다: `run.startedAt.formatted(.dateTime.year().month(.wide).day().locale(Locale(identifier: "ko_KR")))`. 여러 파일이 같은 패턴이니 RunMath에 `static let korean = Locale(identifier: "ko_KR")` 하나 두고 재사용하면 나머지도 한 줄씩 고칠 수 있다.

### `RunCanvas/Features/Courses/CourseDetailView.swift`

- **'이 코스로 달리기' 시작 버튼이 PrimaryButton 을 안 써 레벨 컬러·모양이 다름** — UI/UX, 46줄
  - 무엇이: `.buttonStyle(.borderedProminent).tint(.primary)`(L46-48). 홈 '러닝 시작'·RunView 는 레벨 컬러 PrimaryButton(라운드 14, 아이콘+제목)인데 코스 따라뛰기 시작만 흑백 borderedProminent(시스템 라운드) 라 같은 행동의 버튼이 두 얼굴이다.
  - 어떻게: `PrimaryButton(title: "이 코스로 달리기", systemImage: "figure.run") { showsRun = true }` 로 교체(L38-48 삭제).

### `RunCanvas/Features/Courses/CourseListView.swift`

- **불러오는 중에 필터를 바꾸면 무시돼 알약과 목록이 어긋난다** — 버그, 60줄
  - 무엇이: `.onChange(of: region/sort/mineOnly)`가 각각 `reload()`를 부르지만 `CourseService.load`(33줄)는 `guard !isLoading else { return }`로 겹치는 호출을 버린다. 첫 로드가 느릴 때 지역을 '부산'으로 고르면 load는 버려지고 `loadRegionCounts`만 돌아, 알약은 '부산 3'인데 목록은 전체 지역 코스가 그대로 남는다. 다시 바꾸기 전엔 맞춰지지 않는다.
  - 어떻게: 세 `onChange` + `.task`를 `.task(id: FilterKey(region, sort, mineOnly))` 하나로 바꿔 SwiftUI가 이전 요청을 취소하게 하고, `CourseService.load`의 `isLoading` 가드를 없애며 결과 대입 전에 `guard !Task.isCancelled`로 늦게 온 응답을 버린다.
- **서버 실패 시 '등록된 코스가 없어요'가 함께 떠서 원인이 뒤바뀐다(지도 모드는 실패가 아예 안 보임)** — UI/UX, 87줄
  - 무엇이: load 실패는 `courses = []` + failureNotice(CourseService 줄 49~50)인데, 목록은 `service.courses.isEmpty`만 보고 ContentUnavailableView('등록된 코스가 없어요 / 기록 상세에서 마음에 든 코스를 올려 보세요')를 실패 안내 바로 아래에 같이 그린다. 두 문구가 서로 모순이고 재시도 버튼도 없다(당겨서 새로고침만 가능). 지도 모드(CourseMapBrowseView 줄 37)는 failureNotice·isLoading을 전혀 안 보고 `courses.isEmpty`면 무조건 '이 지역에는 아직 코스가 없어요'를 띄운다 → 첫 로딩 중과 네트워크 실패가 모두 '코스 없음'으로 보인다.
  - 어떻게: CourseListView 줄 87 조건을 `service.courses.isEmpty && service.failureNotice == nil`로 바꾸고, 실패 안내 Label 옆에 `Button("다시 시도") { Task { await reload() } }`를 붙인다. CourseMapBrowseView 줄 37은 `service.isLoading` → '불러오는 중…', `service.failureNotice` → 그 문구, 그 외 → 기존 문구로 분기한다(service는 이미 넘겨받고 있다).

### `RunCanvas/Features/Courses/CourseMapBrowseView.swift`

- **지도 핀 터치 영역이 약 23pt라 누르기 어렵다** — UI/UX, 58줄
  - 무엇이: 핀 버튼 라벨은 11pt 아이콘 + 6pt 패딩(선택 시 15+9)이라 히트 영역이 23~33pt로 44pt 기준에 못 미친다. `.buttonStyle(.plain)`이라 라벨 크기가 곧 히트 영역이다. 지도 위 작은 점을 손가락으로 겨냥해야 하는 화면이라 실패가 잦고, 빗나가면 지도 팬으로 먹혀 '눌렀는데 반응이 없다'로 느껴진다.
  - 어떻게: 라벨의 Image 체인 뒤에 `.frame(width: 44, height: 44).contentShape(Circle())`를 붙인다(보이는 원은 그대로, 히트 영역만 44pt). 선택 시 크기 변화는 안쪽 padding으로 유지된다.

### `RunCanvas/Features/Courses/CourseRegisterView.swift`

- **'너무 짧다'는 걸 이름까지 다 적고 등록을 누른 뒤에야 알려주고, '경로 점 N개'는 사용자에게 무의미** — UI/UX, 38줄
  - 무엇이: CourseService.register(줄 76~77)가 trimmed가 비면 tooShort를 던지므로, 600m 미만 기록은 시트를 열고 이름을 입력하고 지역을 고른 뒤 '등록'을 눌러야 비로소 '기록이 짧아요'를 본다(RunDetailView 줄 70은 `route.count > 1`만 본다). 또 시트의 두 번째 섹션 '경로 점 412개'는 내부 데이터 개수라 사용자가 판단할 정보가 아니고, 정작 궁금한 '앞뒤 150m를 자르면 얼마가 공유되는가'는 안 보인다.
  - 어떻게: CourseRegisterView에 `@State private var shared: [CoursePoint] = []`를 두고 `.task`에서 `CourseGeometry.trimmed(CourseGeometry.path(from: run.route))`를 한 번 계산. '경로 점' 행을 `LabeledContent("공유되는 거리", value: "%.2f km" of CourseGeometry.length(shared))`로 바꾸고, `shared.isEmpty`면 footer에 CourseError.tooShort.errorDescription을 보여주며 canSubmit을 false로. register에 넘길 때도 이미 계산한 shared를 쓰면 이중 계산이 없다.
- **업로드 중 취소·스와이프가 가능해 취소했는데도 코스가 올라간다** — UI/UX, 57줄
  - 무엇이: `isUploading` 동안 스피너만 얹을 뿐 '취소' 버튼과 시트 스와이프 내리기는 살아 있고 Task는 계속 돌아, 사용자가 취소했다고 믿는 코스가 서버에 등록되고 부모의 '코스로 등록했어요' 알럿까지 뜬다. 별개로 `guessRegion`(79줄)은 사용자가 이미 지역을 바꿨어도 지오코딩이 늦게 오면 덮어쓴다.
  - 어떻게: `.interactiveDismissDisabled(isUploading)`, 취소 버튼 `.disabled(isUploading)`. guessRegion은 시작 시 `let initial = region`을 잡고 `if region == initial { region = guessed }`로 사용자가 안 건드렸을 때만 채운다.

### `RunCanvas/Features/Home/RunnerHomeView.swift`

- **새 기기 첫 로그인 직후 '아직 기록이 없어요. 첫 러닝을 시작해 보세요.'가 떴다가 기록이 쏟아진다** — UI/UX, 108줄
  - 무엇이: AppRouter.load()는 isLoading=false로 홈을 띄운 뒤 syncIfPossible()로 pullRuns를 시작한다(줄 109~110). 재설치·기기 교체 사용자는 홈에서 '기록이 없어요, 첫 러닝을 시작해 보세요'를 먼저 보고 몇 초 뒤 기록이 한꺼번에 나타난다. 백업이 사라진 줄 알고 놀라기 딱 좋은 순간인데 복원 중이라는 신호가 어디에도 없다(syncedAt을 화면에 쓰는 곳도 없음).
  - 어떻게: SyncService에 `@Observable final class State { var isRestoring = false }; static let state = State()`를 두고 pullRuns 시작/끝에서 토글. 홈 줄 108은 `SyncService.state.isRestoring ? "서버에서 기록을 불러오는 중…" : "아직 기록이 없어요. 첫 러닝을 시작해 보세요."`. (더 짧은 대안: AppRouter에서 profiledUserKey가 이 계정과 다를 때—즉 이 기기 첫 로그인—만 `pullMissing`을 스플래시 안에서 먼저 await.)

### `RunCanvas/Features/Marathon/MarathonService.swift`

- **씨앗·캐시가 있는데 서버 응답까지 빈 화면에 '불러오는 중'만 보인다** — UI/UX, 22줄
  - 무엇이: load()는 서버 응답이 오거나 실패한 뒤에야 events를 채운다. 번들에 197개 일정(marathons.json), 두 번째 실행부터는 캐시가 이미 있는데도 느린 망에서는 몇 초 동안 화면 전체가 ContentUnavailableView('불러오는 중')다(MarathonScheduleView 줄 42·87). 데이터가 손에 있는데 스피너를 보여주는 셈이고, 응답이 오면 전체가 통째로 바뀌어 깜빡인다. 실패 시 fallback 문구는 잘 돼 있지만 성공 경로의 첫 몇 초가 비어 있다.
  - 어떻게: load() 시작 부분에 `if events.isEmpty, let quick = Self.readCache().flatMap({ $0.isEmpty ? nil : $0 }) ?? nonEmpty(MarathonSchedule.bundled().events) { events = quick }`(fallbackNotice는 건드리지 않음)를 넣고 그다음 서버를 받아 교체한다. 그러면 스피너는 진짜 첫 실행·캐시 없음일 때만 보인다.
- **캐시가 있어도 서버 실패 때만 읽어, 화면에 들어올 때마다 빈 '불러오는 중' 을 본다** — UI/UX, 27줄
  - 무엇이: MarathonScheduleView 는 NavigationLink 로 매번 새로 만들어져 @State service 도 새로 생기고(6행), load() 는 서버 응답이 올 때까지 events 가 비어 있다(27-40행). readCache 는 catch 에서만 호출된다(49-50행). 즉 디스크에 방금 전 목록이 있어도 느린 망에서는 몇 초 동안 달력 아이콘의 '불러오는 중' 만 보이고, 응답이 오면 목록이 한꺼번에 튀어나온다. 캐시 파일(135KB)은 이미 만들어 두고 있어 읽기만 추가하면 된다.
  - 어떻게: load() 의 do 앞에: if events.isEmpty, let cached = Self.readCache(), !cached.isEmpty { events = cached } — 캐시를 먼저 보여주고 서버 결과로 덮어쓴다(stale-while-revalidate). 실패 시 catch 의 loadFallback 은 그대로 두면 안내 문구가 붙는다.

### `RunCanvas/Features/Running/RouteMapView.swift`

- **기록 상세를 열 때마다 '경로 없음' 오버레이가 잠깐 떴다 사라짐** — UI/UX, 46줄
  - 무엇이: `coords`는 `@State`로 빈 배열에서 시작해 `.task(id:)`에서 채워진다. 빈 상태 오버레이 조건이 `coords.count < 2`라, 경로가 있는 기록도 첫 렌더(push 애니메이션 초반)에는 `.regularMaterial` 위에 '경로 없음 / GPS 경로가 기록되지 않았어요'가 그려졌다가 task가 끝나면 사라진다. task가 메인 액터에서 `PaceSegment.build`(포인트마다 CLLocation 할당)를 돌리므로 긴 기록일수록 그 깜빡임이 길다. 잘못된 빈 상태를 매번 보여주는 의미 없는 깜빡임이다.
  - 어떻게: 오버레이 조건을 입력값 기준으로 바꾼다: RouteMapView:46 `if coords.count < 2` → `if route.count < 2`(경로 유무는 route로 바로 알 수 있고, coords는 지도 그리기 전용으로 남긴다). 36행 범례 조건도 `route.count > 1`로 맞춘다.

### `RunCanvas/Features/Running/RunCoordinator.swift`

- **워치 앱이 화면에 떠 있어야만 워치 기록이 되는데 폰은 아무 안내 없이 'iPhone 기록'으로 시작한다** — UI/UX, 56줄
  - 무엇이: iOS 쪽 WCSession.isReachable은 워치 앱이 포그라운드(또는 워크아웃 중 백그라운드)일 때만 true다. 손목을 내려 워치 앱이 잠들면 폰 RunView 상단 라벨이 'iPhone 기록'으로 바뀌고 start()는 .start 명령조차 보내지 않는다. 사용자는 워치를 차고 있는데 왜 심박이 안 잡히는지 알 길이 없고, '워치 앱을 먼저 열어야 한다'는 힌트가 어디에도 없다. HealthKit에는 폰에서 워치 앱을 띄우는 API(HKHealthStore.startWatchApp(with:))가 있고, 실패 시 폰으로 넘겨받는 15초 타임아웃(watchStartTimeout)이 이미 구현돼 있어 붙이기만 하면 된다.
  - 어떻게: usesWatchWorkout = sendToWatch ? watch.isWatchAppInstalled : true 로 바꾸고, 설치돼 있지만 !isReachable이면 HKWorkoutConfiguration(.running/.outdoor)으로 healthStore.startWatchApp(with:)를 Task로 호출한 뒤 기존처럼 sendCommand(.start) (transferUserInfo라 앱이 뜨면 배달됨). 실패는 기존 checkWatchAlive 15초 인계가 그대로 처리한다.
- **takeOver 뒤에도 워치 스냅샷의 심박을 계속 기록하고, 종료 시 폰·워치가 각각 HealthKit 워크아웃을 저장한다** — 버그, 120줄
  - 무엇이: checkWatchAlive(:154)는 30초 무소식이면 takeOver→session.takeOverHealthWorkout()으로 폰 HKAnchoredObjectQuery 심박 스트림을 켠다. 그런데 워치는 그 사실을 모르고 계속 돌며, 블루투스가 잠깐(30초+) 끊겼다 붙으면 스냅샷이 다시 온다. apply(:117-124)는 healthManagedExternally를 보지 않고 매 스냅샷(워치 타이머 1초 주기)마다 recordHeartRate를 호출해 폰 스트림 샘플과 이중으로 쌓인다 → 평균·최대 심박이 왜곡된다. finish 시 RunSession.finish(:132)는 healthManagedExternally == false가 됐으니 폰 워크아웃을 HealthKit에 저장하고, 워치의 finishWorkout()도 저장해 건강 앱에 같은 러닝이 두 개 생긴다.
  - 어떻게: apply(): `guard session.healthManagedExternally` 를 심박 기록 앞에 추가(lastWatchSnapshotAt 갱신은 그 앞에 둬도 됨). 워치 워크아웃을 아예 정리하려면 takeOver()에서 `watch.sendCommand(.end, sessionID: session.sessionID)`도 보내는 방법이 있으나, 최소 diff는 guard 하나.

### `RunCanvas/Features/Running/RunDetailView.swift`

- **큰 글씨에서 페이스·시간 값이 글자 단위로 줄바꿈됨 (StatLabel 4열)** — UI/UX, 40줄
  - 무엇이: RunDetailView는 `HStack(spacing: 0)`에 StatLabel 4개(시간·페이스·칼로리·평균 BPM)를 넣고 좌우 24pt 여백을 쓴다. iPhone SE(375pt)에서 열 하나가 약 82pt인데 StatLabel 값은 `.font(.headline)`에 lineLimit·minimumScaleFactor가 없다. 접근성 크기 AX1(headline 28pt)부터 `12'34"` 같은 값이 82pt를 넘어 `12'3` / `4"`처럼 중간에서 끊기고, 캡션 '평균 BPM'도 두 줄이 된다. RunView(3열+Divider)와 RunStatsView(3열)도 같은 컴포넌트를 써서 큰 사이즈에서 함께 깨진다.
  - 어떻게: Components/StatLabel.swift의 값 Text에 `.lineLimit(1).minimumScaleFactor(0.6)`을, 제목에 `.lineLimit(1).minimumScaleFactor(0.8)`을 붙인다(공용 컴포넌트 한 곳 수정으로 RunView·RunDetailView·RunStatsView가 같이 고쳐진다). 값에 `.monospacedDigit()`도 함께 붙여 초가 바뀔 때 폭이 흔들리지 않게 한다.

### `RunCanvas/Features/Running/RunView.swift`

- **큰 화면 제목이 28/30/30/32 네 가지 크기, RunView 는 내비바 위에 제목을 또 그림** — UI/UX, 84줄
  - 무엇이: ProfileSetupView L38 `size: 28`, LoginView L26 `30`, RunResultView L32 `30`, SplashView L23 `32`. RunView 는 홈에서 push 되어 내비바가 있는데도 `Text("러닝").font(.title2).bold()`(L84) 를 본문 상단에 직접 그려 뒤로가기 줄 아래 제목이 하나 더 생기고, 같은 종류의 화면인 TrainingRunView 는 `.navigationTitle` inline(L78-79) 이라 러닝 화면 둘의 머리가 다르다.
  - 어떻게: 모달·온보딩 제목은 `.largeTitle.bold()` 하나로 통일. RunView 는 L83-93 HStack 을 지우고 `.navigationTitle("러닝").navigationBarTitleDisplayMode(.inline)` + Watch/iPhone 라벨을 `.toolbar` trailing 으로.
- **러닝 중 GPS 상태 표시가 없음 — 신호를 못 잡아도 0.00km에 타이머만 돈다** — UI/UX, 103줄
  - 무엇이: LocationService.isUsable은 정확도 20m 이내, 첫 fix는 10초 이내 신선한 것만 받는다. 건물 사이·실내 출발에서는 첫 유효 fix까지 수십 초가 걸리는데, 화면은 거리 0.00과 흐르는 시간뿐이라 사용자는 'GPS를 못 잡은 건지, 앱이 고장난 건지' 알 수 없다(로딩 상태 누락). `session.currentLocation`(유효 fix가 들어올 때만 갱신)과 `session.locationAuthorization`이 이미 노출돼 있어 상태를 만들 재료는 다 있다.
  - 어떻게: RunView:106 'km' 아래에 한 줄 캡션을 조건부로 넣는다: `if isActive, session.currentLocation == nil { Text(session.locationAuthorization == .denied || .restricted ? "위치 권한이 꺼져 있어 거리가 기록되지 않아요" : "GPS 신호를 찾는 중…").font(.caption).foregroundStyle(.secondary) }`. 스피너 없이 텍스트만 — 유효 fix가 오면 자연스럽게 사라진다.
- **RunView가 다시 뜰 때마다 restartHeartRateStream()이 앵커 쿼리를 새로 걸어 러닝 시작 이후 심박 샘플이 전부 중복 기록됨** — 버그, 158줄
  - 무엇이: RunView는 화면 인스턴스마다 @State `didRequestHealthAuthorization`이 false로 시작하므로, 러닝 중 탭을 옮겼다가 돌아오면 .task가 다시 돌아 `session.restartHeartRateStream()`(158행)을 부른다. RunSession.restartHeartRateStream(220~225행)은 상태만 확인하고 HealthService.startHeartRateStream을 다시 호출하는데, 이 함수는 `anchor: nil`(HealthService 83행)로 HKAnchoredObjectQuery를 새로 만들어 초기 결과 핸들러가 startedAt 이후 모든 심박 샘플을 다시 배달한다 → `recordHeartRate`가 같은 샘플을 또 append. 예: 30분 러닝 중 20분에 기록 탭을 봤다 돌아오면 0~20분 구간이 두 번 세어져 averageHeartRate가 앞 구간으로 쏠린다. 워치 인계(takeOverHealthWorkout)도 워치가 이미 스냅샷으로 넣은 심박을 HealthKit 동기화본으로 다시 받아 같은 중복이 생긴다.
  - 어떻게: 최소 수정: RunSession에 `private var didRestartHeartRateStream = false`를 두고 start()에서 false로 리셋, restartHeartRateStream()에서 `guard !didRestartHeartRateStream`로 러닝당 한 번만 재시작한다(권한이 늦게 오는 경우는 첫 RunView.task에서 처리됨). 근본 수정: HealthService가 `deliveredThrough: Date`를 들고 startHeartRateStream(since:)에서 since가 같으면 유지, deliver에서 `startDate > deliveredThrough`인 샘플만 넘기고 갱신하면 재시작·인계 모두 중복이 사라진다.
- **위치 권한 거부 처리가 RunView.startRun 한 곳에만 있어 프롬프트 거부·훈련·워치 시작은 0.00km를 조용히 기록함** — 버그, 229줄
  - 무엇이: startRun()은 이미 `.denied/.restricted`일 때만 알럿을 띄우고 시작을 막는다(231~232행, 주석: "0.00km를 저장하게 두지 않기 위해"). 그러나 (a) 첫 러닝에서 `.notDetermined` 상태로 runs.start()가 먼저 실행되고 사용자가 시스템 프롬프트에서 "허용 안 함"을 고르면 세션은 계속 .running이고 `session.locationAuthorization` 변화를 아무도 보지 않아 거리 0으로 달리다 저장된다. (b) 게이트가 뷰에 있어서 TrainingRunView.onAppear(`runs.start()`, 81행)와 워치 시작(RunCoordinator.handle .start)은 권한이 거부돼 있어도 그대로 시작해 0km 기록을 남긴다. 주석의 의도와 달리 세 경로에서 0.00km 기록이 저장된다.
  - 어떻게: 게이트를 공용 지점인 RunCoordinator.start로 옮겨 `@discardableResult func start(...) -> Bool`로 만들고 `session.locationAuthorization`이 거부/제한이면 false를 돌려 호출자(RunView·TrainingRunView)가 알럿을 띄우게 한다(finish와 같은 계약). RunView에는 `.onChange(of: session.locationAuthorization)`을 추가해 러닝 중 거부로 바뀌면 `showsLocationDenied = true`를 띄운다(취소/버리기 없이 종료만 있으니 최소한 사용자가 알게는 해야 한다).

### `RunCanvas/Features/Training/ProgramEditorView.swift`

- **세션을 지울 수단이 사실상 없다 (Section에 건 onDelete)** — UI/UX, 70줄
  - 무엇이: `ForEach($draft.sessions) { Section { … } }.onDelete`는 List가 Section을 행으로 다루지 않아 스와이프 삭제 UI가 생기지 않는다(시뮬레이터에서 세션 헤더/이름 행을 스와이프해 확인). 결과적으로 잘못 추가한 세션은 지울 수 없고, 구간을 전부 지우면 `canSave`가 false가 돼 '저장'만 꺼진 채 이유가 없다.
  - 어떻게: 각 세션 Section 끝에 `Button("세션 삭제", role: .destructive)`(구간이 2개 이상이면 confirmationDialog)을 두고 죽은 `.onDelete`는 지운다. 구간이 없는 세션엔 footer로 "구간이 하나 이상 필요해요"를 보여 준다.

### `RunCanvas/Features/Training/TrainingProgramDetailView.swift`

- **편집·저장 뒤에도 상세 화면은 옛 프로그램을 보여 준다** — UI/UX, 70줄
  - 무엇이: `program`이 `let`이라 편집 시트에서 `TrainingStore.save`로 저장해도 이름·세션 목록이 갱신되지 않는다. 목록(TrainingProgramListView)은 pop 후 onAppear로 다시 읽지만, 사용자가 실제로 보는 상세는 그대로라 저장이 안 된 것처럼 보이고, 갱신 전 '여기서 시작'을 누르면 옛 세션으로 훈련이 시작된다.
  - 어떻게: `@State private var program`으로 바꾸고 `.sheet(isPresented:onDismiss:)`에서 `TrainingStore.customPrograms(ownerID:).first { $0.id == program.id }`로 다시 읽어 넣는다.
- **편집 시트에서 저장해도 상세 화면은 수정 전 프로그램을 계속 보여줌** — 버그, 70줄
  - 무엇이: `let program`은 목록에서 push될 때의 값 복사본이고, ProgramEditorView는 TrainingStore에만 저장한다. 세션 이름·구간·프로그램 이름을 바꾸고 '저장'을 누르면 시트는 닫히지만 뒤의 상세 화면(제목, 세션 카드, '이어서 하기', 주차 묶음)은 옛 내용 그대로다. 뒤로 나갔다 다시 들어와야 반영된다. 사용자는 저장이 안 된 줄 알고 다시 편집하게 된다.
  - 어떻게: `@State private var program: TrainingProgram`으로 바꾸고(init에서 초기화), `.sheet(isPresented:, onDismiss:)`의 onDismiss에서 `if let saved = TrainingStore.customPrograms(ownerID: ownerID).first(where: { $0.id == program.id }) { program = saved }`로 다시 읽는다. 최소 diff로 끝난다.

### `RunCanvas/Models/Badge.swift`

- **잠긴 뱃지 캡션이 과거형("달렸어요", "완주했어요")이라 이미 딴 것처럼 읽힌다** — UI/UX, 83줄
  - 무엇이: BadgesView.caption은 잠김 상태의 `.firstRun`(274)과 `.time` 카테고리(282)에 `badge.detail`을 그대로 쓴다. 그래서 아직 못 딴 셀에 "첫 러닝을 완주했어요", "아침 6시 전에 달렸어요", "밤 9시 이후에 달렸어요"가 흑백 이미지 아래 찍힌다 — 사용자는 딴 건지 못 딴 건지 헷갈리고, 획득 셀은 어차피 날짜("… 획득")를 보여줘서 과거형 문구가 실제로 쓰이는 곳은 잠김 셀뿐이다. 같은 enum의 거리·누적·횟수 detail은 이미 "한 번에 5km 이상"처럼 중립형이라 톤도 안 맞는다.
  - 어떻게: Badge.detail의 과거형 5개를 조건 문구로 바꾼다: `.firstRun` "첫 러닝 완주", `.streak3/7/30` "3일 연속 러닝"…, `.earlyBird` "아침 6시 전 러닝", `.nightRunner` "밤 9시 이후 러닝". BadgesView 쪽 코드는 손대지 않아도 된다(detail은 다른 곳에서 안 쓴다).

### `RunCanvas/Services/CourseGeometry.swift`

- **왕복·순환 코스에서 진행도가 뒤집히거나 0%로 떨어짐(가장 가까운 점을 전체에서 찾음)** — 버그, 80줄
  - 무엇이: progress/offCourseMeters는 nearestIndex로 경로 전체에서 최단 점을 찾는다. 러닝 코스의 대부분인 왕복(강변 갔다 오기)은 복귀 구간 점이 출발 구간 점과 겹치므로, 돌아오는 동안 최단 점이 출발 구간(작은 index)과 복귀 구간(큰 index) 사이에서 GPS 오차에 따라 왔다 갔다 한다 → RunView의 %와 '코스 x km 중 y km'가 50%↔90% 사이를 튀고, 도착해도 0~10%로 끝난다. 순환 코스는 trimmed된 출발·도착이 가까우면 마지막 근처에서 index 0이 잡혀 100% 직전에 0%가 된다. 주석의 '되돌아 뛰면 값이 줄어든다'는 역주행을 뜻하지, 정상 왕복까지 의도한 게 아니다. 테스트(CourseFollowTests)는 직선 경로만 다룬다.
  - 어떻게: 이전 매칭 index 근처에서만 찾는 창(window) 검색을 추가한다: `nearestIndex(to:in:near lastIndex:window: 40)`로 `lastIndex...min(lastIndex+window, count-1)` 범위에서 최단 점을 찾고, 그 거리가 50m를 넘을 때만 전체 검색으로 폴백(코스 이탈/복귀). RunView는 `@State lastCourseIndex`를 들고 updateCourseFollow에서 넘긴다. 왕복 경로(직선을 이어 붙여 되돌아오는 path)로 진행도가 단조 증가해 1에 닿는 테스트 하나 추가.

### `RunCanvas/Services/CourseService.swift`

- **로딩 중 바뀐 지역·정렬 요청이 버려져 칩과 목록이 어긋난다** — UI/UX, 33줄
  - 무엇이: `guard !isLoading else { return }`는 진행 중인 load가 있으면 새 요청을 조용히 버린다. CourseListView는 region/sort/mineOnly가 바뀔 때마다 `reload()`를 호출하므로(60-62줄), 첫 로드가 끝나기 전에 지역 칩을 '서울'로 바꾸면 요청이 무시되고 '전체 지역' 결과가 '서울' 칩 아래 표시된다. 초기 로드 중 pull-to-refresh도 즉시 리턴돼 스피너만 끝난다. 사용자가 다시 칩을 눌러야만 복구된다.
  - 어떻게: guard를 없애고 세대 카운터로 바꾼다: `private var loadID = 0`; load 시작에서 `loadID += 1; let id = loadID`, 응답을 받은 뒤 `guard id == loadID else { return }`를 거쳐야 `courses`/`failureNotice`를 대입한다. `isLoading`은 UI 표시용으로만 남긴다.
- **50개 이후 코스는 영원히 볼 수 없고, 헤더 '50개'와 알약 '(73)'이 다르게 나온다** — UI/UX, 43줄
  - 무엇이: `.limit(50)`만 있고 다음 페이지를 받는 길이 없다. 서울처럼 코스가 50개를 넘는 지역에서는 '최신순'이면 오래된 코스, '짧은 코스부터'면 긴 코스가 어떤 조작으로도 안 보인다(막다른 목록). 동시에 CourseListView 줄 25는 `service.courses.count`로 '50개'를 적는데, 같은 줄의 FilterMenuPill은 regionCounts로 '서울 73'을 보여줘 숫자가 어긋난다. 필터를 바꿀 때마다 응답 전까지 '0개'로 깜빡이기도 한다.
  - 어떻게: 헤더는 `service.regionCounts[region] ?? service.courses.count`를 쓴다. CourseService에 `func loadMore()` 추가: 같은 필터로 `.range(from: courses.count, to: courses.count + 49)` 받아 append. 목록 끝에 `courses.count < (regionCounts[region] ?? 0)`일 때 '더 보기' 버튼(Color.card 카드)을 둔다.

### `docs/supabase/schema.sql`

- **courses에 크기·길이 제약이 없어 한 사용자가 목록을 모두에게 망가뜨릴 수 있다** — 보안, 113줄
  - 무엇이: `courses`는 publishable 키 + 로그인만 있으면 insert 가능하고, name 길이·path 원소 수·distance_m 범위에 check가 없다. 앱 클라이언트는 5m 간격으로 솎지만(CourseGeometry.path) 서버는 그 가정을 강제하지 않는다. 경로 수십만 점짜리 행 하나를 넣으면 `.limit(50)` 목록 조회가 그 지역 모든 사용자에게 수십 MB가 되고, 2,000자 이름은 카드(fixedSize vertical)를 화면 하나 크기로 늘린다. `owner_nickname`도 클라이언트 값 그대로라 남의 닉네임으로 올릴 수 있다. 클라이언트도 이름 길이 제한이 없고 canSubmit(줄 20)은 `.whitespaces`만 다듬어 줄바꿈만 있는 이름이 통과한다(register는 `.whitespacesAndNewlines`).
  - 어떻게: 새 migration: `alter table public.courses add constraint courses_name_len check (char_length(name) between 1 and 40), add constraint courses_path_size check (jsonb_array_length(path) between 2 and 5000), add constraint courses_distance check (distance_m between 300 and 100000);` + `before insert` 트리거로 `owner_nickname := coalesce((select nickname from public.profiles where id = auth.uid()), '')`. 클라이언트: CourseRegisterView TextField에 `.onChange(of: name) { name = String($0.prefix(40)) }`, canSubmit도 `.whitespacesAndNewlines`로 통일.

## 심각도 낮음 (41건)

### `RunCanvas/App/AppRouter.swift`

- **AppRouter가 재생성될 때마다 AuthService가 새로 만들어져 authStateChanges 구독 Task가 누적된다** — 성능, 6줄
  - 무엇이: `@State private var auth = AuthService()`의 초기값 식은 `AppRouter()` 구조체가 만들어질 때마다 평가되고 SwiftUI는 첫 번째만 쓴다. `AppRouter()`는 RunCanvasApp.body가 재평가될 때마다 생성되는데, body는 `@StateObject watchConnectivity`의 `@Published`(isPaired·isWatchAppInstalled·isReachable, WatchConnectivityService.swift:23-25)가 바뀔 때마다 재평가된다 — 워치 화면이 꺼지고 켜질 때마다. 버려진 AuthService 인스턴스마다 `init()`의 `Task { for await … in supabase.auth.authStateChanges }`가 남는다: `[weak self]`라 self는 해제되지만 루프는 스트림이 끝나지 않으므로 영원히 살아 SDK 이벤트 이미터에 continuation이 쌓인다. 긴 러닝 동안 워치 reachability가 수십 번 바뀌면 그만큼 누적.
  - 어떻게: 루프 안에 `guard let self else { return }`을 넣어 버려진 인스턴스의 Task가 다음 이벤트에서 끝나게 하고, 근본적으로는 RunCoordinator처럼 RunCanvasApp.init에서 AuthService를 한 번 만들어 `.environment(auth)`로 내려보낸다(AppRouter의 @State 제거).

### `RunCanvas/Features/Auth/ProfileSetupView.swift`

- **고정 폰트 크기라 Dynamic Type이 적용되지 않음** — UI/UX, 38줄
  - 무엇이: "프로필 설정" 제목이 `.font(.system(size: 28, weight: .bold))`로 고정돼 큰 글씨 설정에서도 커지지 않는다. LoginView.swift:23,26과 SplashView.swift:19,23,26도 같은 방식이다(로고 마크는 브랜드 요소라 넘어가더라도 페이지 제목은 텍스트 스타일이어야 한다).
  - 어떻게: ProfileSetupView 제목을 `.font(.largeTitle.bold())`로, LoginView/SplashView의 앱 이름은 `.font(.largeTitle.bold())`, 태그라인은 `.font(.subheadline)`로 바꾼다.
- **프로필 설정 폼이 VoiceOver 라벨이 없고 프로필 편집 화면과 모양이 다름** — UI/UX, 130줄
  - 무엇이: SetupRow는 제목 Text와 TextField를 HStack에 나란히 둔 구조(140-155)라 TextField에 접근성 라벨이 붙지 않는다 — VoiceOver는 닉네임·키·체중 세 칸을 모두 "텍스트 필드"로만 읽는다. 같은 정보를 편집하는 ProfileEditView는 `LabeledContent`(39-47)로 라벨이 자동 연결되고, 인셋 그룹 List + Color.card 셀을 쓴다. 두 화면은 하단 버튼 여백·배경도 다르다(설정 24pt + `.background(.background)` 94-96행 vs 편집 20pt + `.background(.bar)` 66-68행).
  - 어떻게: ProfileSetupView 본문을 ProfileEditView와 같은 `List { Section { LabeledContent("닉네임") { TextField } … } }.listStyle(.insetGrouped).appListTone()` 구조로 바꾸고 SetupRow를 없앤다(hint는 Section footer로). 최소 수정만 원하면 TextField에 `.accessibilityLabel(title)`만 추가하고 하단 인셋 여백·배경을 편집 화면과 맞춘다.

### `RunCanvas/Features/Badges/BadgeEarnedToast.swift`

- **새 뱃지 토스트가 VoiceOver에 알려지지 않고 3초 뒤 사라지며 탭으로 닫을 수도 없다** — UI/UX, 40줄
  - 무엇이: 오버레이로 뷰가 추가될 뿐 접근성 알림이 없어 VoiceOver 사용자는 3초 안에 포커스를 옮기지 않으면 "새 뱃지 3개를 땄어요"를 놓친다. 시각 사용자도 제목 3개("첫 러닝 · 5K · 얼리버드")를 읽기엔 3초가 빠듯한데 눌러서 닫거나 잡아 둘 방법이 없다(탭·스와이프 무반응).
  - 어떻게: BadgeEarnedToast 본문에 `.onAppear { AccessibilityNotification.Announcement("새 뱃지 \(badges.count)개: \(badges.map(\.title).joined(separator: ", "))").post() }`를 추가하고, 확장의 오버레이에 `.onTapGesture { withAnimation { badges.wrappedValue = [] } }`를 붙여 탭으로 닫게 한다.

### `RunCanvas/Features/Badges/BadgesView.swift`

- **섹션 제목→내용 간격·섹션 사이 간격이 화면마다 다름** — UI/UX, 47줄
  - 무엇이: `.headline` 제목 아래 간격: 홈·기록 16, 뱃지 12(L47, L88, L108), 트레이닝 14(TrainingProgramListView L47), RunDetail '나의 런꾸' 10(L25). 섹션 사이: 홈 28(L49)·뱃지 28(L38)·기록 24(RunStatsView L59)·런꾸 20(CanvasHomeView L39)·정산 18(RecapView L29).
  - 어떻게: 제목→내용 16, 섹션 사이 24 하나로 맞춘다.

### `RunCanvas/Features/Canvas/CanvasBackgroundSheet.swift`

- **사진 불러오는 중 프리셋을 누르면 나중에 도착한 사진이 선택을 덮어쓴다** — 버그, 38줄
  - 무엇이: 앨범·카메라 버튼은 `isLoadingPhoto`로 잠기지만(30·35행) 프리셋 그리드는 안 잠긴다. 사진 로딩(최대 20초 타임아웃) 중 프리셋을 누르면 `onSelect(.preset)`→`dismiss()`가 먼저 가고, 뒤늦게 Task가 끝나면서 `onSelect(.photo)`가 한 번 더 불려 편집기 배경이 사용자가 고른 프리셋 대신 사진으로 바뀐다.
  - 어떻게: `LazyVGrid`에 `.disabled(isLoadingPhoto)` 한 줄 추가 (소스 버튼 두 개와 같은 규칙).
- **Color.card 보조 버튼이 다섯 벌 복사돼 있고 한 벌만 폰트·패딩이 다름** — UI/UX, 117줄
  - 무엇이: RunDetailView L55-60, CanvasHomeView L48-53, RecapView L96-101, CanvasExportSheet L100-106 은 `.headline` + `.padding()` + Color.card + 라운드 14 로 같은데, 여기 sourceButton 만 `.subheadline.weight(.semibold)`(L117) + `.padding(.vertical, 14)`(L120) 이다.
  - 어떻게: Components/ 에 `SecondaryButton(title:systemImage:action:)` 하나(Color.card, 라운드 14, .headline)를 만들고 다섯 곳이 쓰게 한다. sourceButton 은 로딩 스피너만 파라미터로.

### `RunCanvas/Features/Canvas/CanvasExportSheet.swift`

- **권한 거부 안내에 '설정 열기'가 없어 사용자가 설정 앱을 직접 찾아가야 함** — UI/UX, 68줄
  - 무엇이: 사진 추가 권한이 꺼져 있으면 `ExportError.notAuthorized`(CanvasExporter:19) "설정에서 사진 추가 권한을 허용해주세요."가 뜨는데 alert 버튼은 '확인' 하나뿐이다(68-70). 카메라도 마찬가지로 CanvasBackgroundSheet.swift:139·143 "설정 앱에서 카메라 권한을 허용해주세요." 뒤에 '확인'(102)만 있다. 두 경우 모두 앱 안에서 다음 행동이 없고, 사용자는 설정 → 앱 → 권한을 손으로 찾아야 한다. iOS 표준 패턴은 alert에 '설정 열기'를 넣는 것.
  - 어떻게: CanvasExportSheet.swift `ExportMessage`에 `var opensSettings = false`를 두고 `saveToPhotos` catch에서 `error as? CanvasExporter.ExportError == .notAuthorized`면 true로; alert actions에 `if message.opensSettings, let url = URL(string: UIApplication.openSettingsURLString) { Link("설정 열기", destination: url) }`. CanvasBackgroundSheet.swift도 `errorMessage`를 같은 식으로 (예: `permissionDenied: Bool` 하나) 확장해 '설정 열기' 추가.

### `RunCanvas/Features/Canvas/CanvasHomeView.swift`

- **저장한 런꾸를 지울 방법이 없음 — 유일한 길이 기록 삭제** — UI/UX, 64줄
  - 무엇이: 갤러리 썸네일은 탭하면 '다시 꾸미기'로만 이어진다(64-68). 마음에 안 드는 런꾸를 없애려면 기록 자체를 지워야(RunListView 스와이프) 하고, 그러면 러닝 기록·통계·뱃지 근거까지 같이 사라진다. 저장 시트에는 '덮어쓰기' 확인까지 있는데 '삭제'는 어디에도 없어, 한 번 '앱에 저장'을 누르면 되돌릴 다음 행동이 없다(이전 리뷰에서도 '저장본 삭제 경로' 미완으로 지적). 삭제 헬퍼 `CanvasStorage.delete(filename:)`는 이미 있다.
  - 어떻게: CanvasHomeView.swift 썸네일 Button에 `.contextMenu { Button("런꾸 삭제", role: .destructive) { … } }` — 액션은 `confirmationDialog`로 한 번 확인 후 `CanvasStorage.delete(filename:)` → `run.decoratedImageFilename = nil` → `thumbnails[run.id] = nil`. `@Environment(\.modelContext)` 한 줄 추가.
- **썸네일이 1080×1350 원본 그대로 메모리에 쌓이고, 디코드는 결국 메인에서 된다** — 성능, 117줄
  - 무엇이: `CanvasStorage.image`는 `UIImage(contentsOfFile:)`라 파일만 열 뿐 디코드는 하지 않는다 — 실제 JPEG 디코드는 그리드에 그릴 때 메인 스레드에서 일어나므로 113행 주석('디코드하므로 메인 밖에서')은 사실과 다르다. 또 `thumbnails`에 런꾸마다 풀사이즈(디코드 시 ~5.8MB)를 들고 있어 저장한 런꾸가 수십 개면 수백 MB로 커진다. 2열 그리드에 필요한 건 ~540px이다.
  - 어떻게: detached 블록 안에서 `await CanvasStorage.image(filename: filename)?.byPreparingThumbnail(ofSize: CGSize(width: 540, height: 675))` 로 바꾸면 백그라운드 디코드 + 축소가 한 줄로 끝난다. (RunDetailView.swift:99도 `preparingForDisplay()`가 맞는 자리 — 범위 밖.)

### `RunCanvas/Features/Canvas/StickerCanvas.swift`

- **날짜 스티커·홈 최근 러닝 날짜가 기기 로케일을 따른다** — 버그, 309줄
  - 무엇이: `run.startedAt.formatted(.dateTime.year().month(.wide).day())`(StickerCanvas.swift:309)와 `formatted(.dateTime.month().day())`(RunnerHomeView.swift:147)는 로케일을 지정하지 않아 영어 기기에서는 'September 6, 2026' / 'Sep 6'로 나온다. 프로젝트 규칙(날짜·요일은 한국어 고정)과 어긋나고, 특히 스티커는 사진 앱에 저장되는 결과물에 그대로 박힌다. 코드베이스에 한국어 로케일 헬퍼는 없고(`ko_KR`은 CourseRegisterView의 지오코더에만), RunDetailView:47·RunStatsView·BadgesView도 같은 패턴이라 이 리뷰 범위 밖에서도 같은 수정이 필요하다.
  - 어떻게: 포맷 스타일에 `.locale(Locale(identifier: "ko_KR"))`를 붙인다. 다섯 군데 이상에서 쓰이니 Theme.swift 옆에 `extension Locale { static let korean = Locale(identifier: "ko_KR") }` 한 줄을 두고 `.locale(.korean)`으로 통일해도 된다.

### `RunCanvas/Features/Courses/CourseDetailView.swift`

- **'이 코스로 달리기'가 PrimaryButton 대신 자체 스타일이라 홈 시작 버튼과 톤이 다르다** — UI/UX, 38줄
  - 무엇이: 러닝을 시작하는 버튼인데 `.borderedProminent` + `.tint(.primary)` + 라운드 기본값(약 8pt)으로 그려서, 홈의 러닝 시작(PrimaryButton: 레벨 컬러, 14pt 라운드)과 다른 화면처럼 보인다. 테마 규칙상 러닝 시작 버튼은 PrimaryButton 하나로 통일돼 레벨 컬러를 받게 돼 있다.
  - 어떻게: `PrimaryButton(title: "이 코스로 달리기", systemImage: "figure.run") { showsRun = true }`로 교체(RootTabView가 넣어 주는 levelTier를 그대로 받는다).

### `RunCanvas/Features/Courses/CourseListView.swift`

- **헤더의 'N개'는 받아 온 50개 한도 개수라 지역 알약의 실제 개수와 다름** — UI/UX, 25줄
  - 무엇이: service.courses는 limit(50)으로 잘린 페이지인데 헤더는 그 count를 '개'로 보여주고, 바로 옆 FilterMenuPill은 regionCounts(최대 1000행 집계)를 붙인다. 한 지역에 코스가 50개를 넘으면 알약은 '서울 120', 헤더는 '50개'가 나란히 뜬다. 지도 보기의 '핀을 눌러 … · N개'(CourseMapBrowseView:46)도 같은 값이다.
  - 어떻게: 헤더와 지도 안내의 개수를 `service.regionCounts[region] ?? service.courses.count`로 바꾼다(내 코스만일 때는 loadRegionCounts가 이미 mineOnly로 집계한다). 또는 50개 초과면 '50개+'로 표시.

### `RunCanvas/Features/Courses/CourseMapBrowseView.swift`

- **지운 코스가 지도에서 여전히 선택돼 있고, 눌러 들어가면 없는 코스 상세로 간다** — 버그, 12줄
  - 무엇이: `selected`는 뷰 상태라 `courses`가 바뀌어도 그대로다. 지도 모드에서 내 코스를 골라 상세 → '이 코스 삭제' → dismiss로 돌아오면 목록에선 사라졌지만 하단 카드와 굵은 폴리라인이 남고, 카드를 누르면 이미 서버에서 지워진 코스의 상세가 다시 열린다(삭제를 또 누르면 '삭제하지 못했어요'). 지역 필터를 바꿔도 이전 지역의 선택 코스가 남는다.
  - 어떻게: `.onChange(of: courses) { if let s = selected, !courses.contains(where: { $0.id == s.id }) { withAnimation { selected = nil } } }`를 Map에 붙인다.

### `RunCanvas/Features/Courses/CourseRegisterView.swift`

- **역지오코딩 결과가 사용자가 이미 고른 지역을 덮어씀** — 버그, 79줄
  - 무엇이: guessRegion은 .task로 비동기 실행되는데 CLGeocoder는 네트워크라 수 초 걸릴 수 있다. 그 사이 사용자가 Picker에서 '경기'를 골랐어도 응답이 오면 `region = guessed`가 무조건 '서울'로 되돌린다. 등록 버튼을 누르기 직전에 바뀌면 잘못된 지역으로 올라간다 — 주석(MarathonSchedule.swift:146)이 막으려던 '잘못 고른 코스가 섞이는' 문제를 반대 방향으로 만든다.
  - 어떻게: await 직전에 `let before = region`을 잡고, 응답 뒤 `guard region == before else { return }`(사용자가 손대지 않았을 때만 적용). 한 줄 추가.

### `RunCanvas/Features/Home/RunnerHomeView.swift`

- **홈 '러닝 시작' 이 PrimaryButton 스타일을 손으로 복사해 드리프트 위험** — UI/UX, 78줄
  - 무엇이: L80-90 이 PrimaryButton.swift L11-18 과 같은 코드(tier?.onAccent, headline, padding, accent, 라운드 14)를 그대로 갖고 있다. Theme.swift L45 주석이 '홈 러닝 시작 버튼' 을 따로 언급하는 이유가 이것이다. 한쪽만 고치면 홈과 러닝 화면의 주요 버튼이 갈라진다.
  - 어떻게: PrimaryButton 의 라벨 부분을 `PrimaryButtonLabel(title:systemImage:)` 로 빼서 PrimaryButton 과 이 NavigationLink 라벨이 같이 쓰게 한다.

### `RunCanvas/Features/Marathon/MarathonScheduleView.swift`

- **필터 칩·알약의 터치 높이가 약 32pt 로 44pt 에 못 미치고 두 줄이 6pt 간격으로 붙어 있다** — UI/UX, 130줄
  - 무엇이: chip(129-140행)·ChipRow(Components/ChipRow.swift 15-24행)·FilterMenuPill(43-47행) 모두 subheadline + 세로 패딩 6 → 높이 약 32pt(스크린샷 92px/3x). 두 줄 사이 spacing 6(106행)이라 손가락이 큰 사용자는 '대회' 를 누르려다 '지역' 이 열린다. 카드 안 종목 칩은 눌리는 것이 아니라 상관없지만 필터 칩은 이 화면의 주 조작이다.
  - 어떻게: 세 곳의 Button 에 .frame(minHeight: 44).contentShape(Rectangle()) 를 붙여 시각은 그대로 두고 히트 영역만 키운다. 두 줄 합쳐 24pt 커지므로 106행 spacing 을 0, 125행 세로 패딩을 4 로 줄이면 필터 블록 높이는 거의 같다.
- **카드를 누르면 앱 밖 Safari 로 나가서, 여러 대회를 비교하려면 매번 앱으로 되돌아와야 한다** — UI/UX, 152줄
  - 무엇이: openURL(152행)은 외부 Safari 를 연다. 이 화면의 유일한 탭 동작이고 상세 화면이 없어, 대회 다섯 개를 훑어보려면 다섯 번 앱을 떠났다가 상태바의 작은 '◀ RunCanvas' 로 돌아와야 한다. 필터 상태는 @State 라 돌아오면 유지되지만 스크롤 위치 복원은 앱 전환에 달려 있다.
  - 어떻게: SafariServices(시스템 프레임워크, 의존성 추가 아님)로 SFSafariViewController 를 감싼 10줄짜리 UIViewControllerRepresentable 을 Components 에 두고 카드에서 .sheet(item: $signupURL) 로 띄운다. 'Done' 으로 목록에 그대로 돌아온다.

### `RunCanvas/Features/Marathon/MarathonService.swift`

- **'연결되면 최신으로 바뀝니다' 안내와 달리 자동 재시도가 없고, 첫 로드 중 당겨서 새로고침은 즉시 끝남** — UI/UX, 52줄
  - 무엇이: 폴백 안내 두 곳(L52, L59)은 연결이 돌아오면 저절로 갱신된다고 말하지만 load() 는 화면 진입(.task)과 pull-to-refresh 에서만 불린다. 사용자는 오프라인 안내를 보고 기다리다 아무것도 안 바뀌는 걸 본다. 또 load() 의 `guard !isLoading` 때문에 첫 조회가 진행 중일 때 당겨서 새로고침하면 곧바로 return 해 스피너가 바로 사라지고, 결과는 나중에 조용히 바뀐다.
  - 어떻게: 문구를 '아래로 당기면 다시 불러와요' 로 고쳐 실제 동작과 맞춘다. 진행 중 재진입은 진행 중인 Task 를 붙잡아 `await task.value` 로 기다리게 하면 스피너가 실제 완료까지 남는다(Task<Void, Never>? 하나 저장).

### `RunCanvas/Features/Recap/RecapView.swift`

- **정산 카드 자리표시자(minHeight 320)가 실제 4:5 카드 높이와 달라 버튼이 아래로 튄다** — UI/UX, 78줄
  - 무엇이: `render`는 `.task`에서 동기로 돌아 첫 프레임만 스피너 → 바로 이미지로 바뀐다. 이때 자리표시자는 320pt인데 렌더된 4:5 이미지는 SE에서 419pt(375-40 폭), Pro Max에서 ~470pt라 "사진 앱에 저장" 버튼이 100pt 이상 아래로 점프하고, `if let rendered`로 감싼 공유 버튼(93)도 그 순간 새로 나타난다. 스피너는 한 프레임짜리라 로딩 상태로서 의미가 없고 깜빡임만 남는다.
  - 어떻게: 자리표시자를 `Color.card`(또는 ProgressView 유지) + `.aspectRatio(CanvasExporter.size.width / CanvasExporter.size.height, contentMode: .fit)`로 바꿔 `minHeight: 320`을 없앤다 — 이미지와 같은 크기를 미리 차지해 레이아웃이 안 움직인다.

### `RunCanvas/Features/Records/RunListView.swift`

- **삭제 시 로컬 save 실패를 삼키고 서버 삭제는 그대로 진행 — 로컬엔 남고 서버 사본만 사라짐** — 버그, 30줄
  - 무엇이: `try? context.save()` 결과와 무관하게 `SyncService.deleteRemote(runIDs:)`가 실행된다. save가 실패하면(스토어 오류·디스크 부족) 다음 실행에서 기록이 로컬에 되살아나지만 서버 행은 이미 지워졌고, 그 기록은 `syncedAt != nil`이라 pushPending이 다시 올리지도 않는다 → 백업 없는 로컬 전용 기록이 된다. 꾸민 이미지(CanvasStorage.delete)도 save 전에 지워져 같은 상황에서 이미지만 잃는다.
  - 어떻게: `do { try context.save() } catch { Logger…error; return }` 뒤에 CanvasStorage.delete와 `Task { await SyncService.deleteRemote(runIDs: ids) }`를 두어 로컬 삭제가 확정된 뒤에만 파일·서버를 지운다.

### `RunCanvas/Features/Running/RouteMapView.swift`

- **경로 지도에 접근성 라벨이 없어 VoiceOver에서 빈 영역으로 읽힘** — UI/UX, 14줄
  - 무엇이: `Map`에 accessibilityLabel이 없고 시작·종료 점은 `.annotationTitles(.hidden)`, 범례는 '빠름'·'느림' 텍스트만 따로 읽힌다. VoiceOver 사용자는 상세 화면 맨 위 260pt가 무엇인지 알 수 없고, '빠름 느림'만 맥락 없이 들린다.
  - 어떻게: RouteMapView:34 아래에 `.accessibilityElement(children: .ignore).accessibilityLabel(route.count > 1 ? "러닝 경로 지도, 구간별 페이스 색상" : "경로 없음")`을 붙이고, PaceLegend에 `.accessibilityHidden(true)`를 준다.

### `RunCanvas/Features/Running/RunCoordinator.swift`

- **ownerID가 없으면 워치 .end가 조용히 실패해 폰 세션이 running으로 영원히 남는다** — 버그, 107줄
  - 무엇이: ownerID는 RunView.onAppear(RunView.swift:164)에서만 쓰인다. 로그인 후 RunView를 한 번도 안 연 상태에서 워치로 러닝을 시작하면 handle(.start)는 ownerID 없이 폰 세션을 시작(GPS 켜짐)하지만, handle(.end)→finish(:76)는 `guard let ownerID else { return false }`로 저장을 못 하고 반환값은 :110에서 버려진다. 알럿은 RunView에서만 뜨므로 사용자는 모르고, 세션은 .running(위치 갱신 계속)에 갇힌다.
  - 어떻게: handle(.start)에 `guard ownerID != nil else { return }`(워치 단독 기록으로 두기) 또는 로그인 성공 시점(AuthService)에서 ownerID를 써 두기. 둘 중 전자가 최소 diff.
- **transferUserInfo로 보낸 .start가 15초 안에 안 닿으면 폰이 인계한 뒤 워치도 워크아웃을 시작해 이중 기록됨** — 버그, 154줄
  - 무엇이: start()는 워치가 reachable이면 `healthManagedExternally = true`로 시작하고 `.start`를 transferUserInfo(백그라운드 큐, 즉시 전달 보장 없음)로 보낸다(55~62행). checkWatchAlive(154~162행)는 스냅샷이 15초 안에 안 오면 takeOver로 폰 HealthKit 기록으로 전환하는데, 이후 늦게 도착한 .start로 워치가 워크아웃을 시작하면 세션ID가 같아 apply(117~124행)가 워치 심박 스냅샷을 계속 받아들이고(폰 HK 스트림과 중복), 종료 시 폰(`!healthManagedExternally` → saveWorkout)과 워치(finishWorkout) 둘 다 건강 앱에 워크아웃을 저장한다. 시작 명령만은 지연이 곧 오판으로 이어지는 구조다.
  - 어떻게: RunCoordinator.apply에서 `guard session.healthManagedExternally`를 추가해 인계 후 워치 심박은 무시하고(중복 방지), 시작 명령은 reachable일 때 sendMessage(errorHandler에서 transferUserInfo로 폴백)로 보내 15초 창이 실제 전달 지연과 맞게 한다. 워치 쪽 워크아웃 중복 저장까지 막으려면 인계 시 워치에 `.end`를 보내는 것도 고려.

### `RunCanvas/Features/Running/RunResultView.swift`

- **결과 화면의 유일한 출구 '홈으로'가 회색 소제목 크기 텍스트 버튼(약 20pt)** — UI/UX, 47줄
  - 무엇이: fullScreenCover라 스와이프로 닫을 수 없고, 나가는 길은 '홈으로'뿐이다. `.font(.subheadline).foregroundStyle(.secondary)`에 `.padding(.vertical, 16)`이 버튼 바깥에 있어 실제 탭 영역은 글자 크기(약 50×20pt)다. 러닝 직후 땀난 손으로 누르기 어렵고 44pt 기준에도 못 미친다.
  - 어떻게: 라벨을 `Text("홈으로").frame(maxWidth: .infinity, minHeight: 44).contentShape(Rectangle())`로 감싸 버튼 안쪽에서 영역을 확보한다(색·글꼴은 그대로). RunView:135 '러닝 종료'도 같은 방식.

### `RunCanvas/Features/Running/RunView.swift`

- **'Watch 기록' 라벨만 초록색 — 흑백 틴트 규칙 밖의 유일한 상태 색** — UI/UX, 91줄
  - 무엇이: `recordsOnWatch ? .green : .secondary`로 워치 연결 시 라벨을 초록으로 칠한다. 테마 규칙상 색은 PrimaryButton의 레벨 컬러뿐이고, 설정 화면의 계정 '연결됨'(SettingsView:223)도 색 없이 텍스트만 쓴다. 같은 '연결 상태' 표시가 화면마다 다른 톤이다. 아이콘(`applewatch…` vs `iphone`)과 문구가 이미 상태를 구분하므로 색이 없어도 정보가 빠지지 않는다.
  - 어떻게: RunView:91을 `.foregroundStyle(.secondary)`로 고정한다(초록 제거). 강조가 필요하면 `.fontWeight(recordsOnWatch ? .semibold : .regular)` 정도로.
- **중단 러닝 알럿 처리 뒤 러닝이 시작되지 않고, 저장했다는 피드백도 없음** — UI/UX, 208줄
  - 무엇이: 홈에서 '러닝 시작'을 눌러 들어왔는데 체크포인트가 있으면 `.task`는 `recovered == nil` 가드로 시작을 건너뛴다. 알럿에서 '저장'/'버리기'를 고른 뒤 화면은 idle 상태의 '러닝 시작' 버튼만 남아, 사용자가 방금 누른 동작을 한 번 더 눌러야 한다. '저장'을 골라도 조용히 저장만 되고(결과 화면·토스트 없음) 알럿 본문 `"1.25km를 저장할까요?"`에는 그 러닝이 언제 것인지도 없다.
  - 어떻게: '저장'·'버리기' 두 버튼의 마지막 줄에 `if startImmediately { startRun() }`을 넣어 선택 직후 러닝을 이어 시작한다. 본문은 `"\(RunMath.formatDateTime(recovered.startedAt))에 시작한 \(RunMath.formatKm(recovered.distanceMeters))km를 저장할까요?"`로 시각을 붙인다(위 날짜 헬퍼 재사용).

### `RunCanvas/Features/Settings/ProfileEditView.swift`

- **올린 프로필 사진을 지울 방법이 없음** — UI/UX, 90줄
  - 무엇이: avatarPicker는 "사진 변경"만 제공한다. 아바타는 공개 버킷에 올라가고(AuthService.swift:106 주석: "URL 아는 사람에게 계속 노출된다") 사용자가 얼굴 사진을 올렸다가 내리고 싶어도 회원 탈퇴 말고는 방법이 없다.
  - 어떻게: avatarURL이 비어 있지 않을 때 PhotosPicker 아래에 `Button("사진 삭제", role: .destructive)`를 추가하고, ProfileService에 `removeAvatar(userID:)`(storage.from("avatars").remove(paths:) — deleteAccount와 같은 경로) 하나를 더해 호출 뒤 `avatarURL = ""`와 avatarURL nil로 upsert한다.

### `RunCanvas/Features/Settings/SettingsView.swift`

- **설정 탭을 열 때마다 목표 거리가 소수 첫째 자리로 반올림돼 다시 저장된다** — 버그, 61줄
  - 무엇이: `onAppear`가 `targetDistanceText = formatted(targetDistance)`를 넣고, `formatted`는 `%.1f`라 5.25 → "5.2". 이어서 `.onChange(of: targetDistanceText)`(line 66-71)가 "5.2"를 파싱해 `targetDistance = 5.2`로 덮어쓴다. 사용자가 입력한 값이 화면을 열기만 해도 바뀐다(0.33 → 0.3 등). 주간 목표도 동일.
  - 어떻게: `formatted`를 `String(format: "%g", value)`로 바꾸면 5 → "5", 5.25 → "5.25"로 왕복이 보존된다(상한 1000이라 지수 표기 걱정 없음). 또는 onChange에서 `Double(text) == 현재값`이면 쓰지 않도록 가드.
- **계정 "해제" 버튼 터치 영역이 글자 크기뿐** — UI/UX, 226줄
  - 무엇이: `Button("해제").buttonStyle(.borderless)`는 텍스트 영역(약 34×22pt)만 탭에 반응한다. 같은 행의 "연결" 버튼은 `.bordered` 캡슐이라 크기가 있는데 해제만 작다. 44pt 권장 터치 영역에 못 미치고 "연결됨" 텍스트 옆에 붙어 있어 오탭이 잦다.
  - 어떻게: "해제"도 "연결"과 같은 `.buttonStyle(.bordered).buttonBorderShape(.capsule).controlSize(.small)`에 `.tint(.red)`를 적용해 크기와 모양을 맞춘다.
- **로그아웃이 확인 없이 즉시 실행됨** — UI/UX, 263줄
  - 무엇이: 빨간 "로그아웃" 행을 한 번 탭하면 바로 signOut되고(264) 로컬 프로필 캐시도 지워진다. 바로 아래 "회원 탈퇴"는 confirmationDialog(80-85)로 확인을 받는데 로그아웃만 없다. 리스트를 스크롤하다 잘못 닿으면 OAuth 웹 플로우를 다시 거쳐 로그인해야 한다.
  - 어떻게: `isConfirmingDelete`와 같은 방식으로 `@State isConfirmingSignOut`을 두고 `.confirmationDialog("로그아웃할까요?")`에서 "로그아웃"(role: .destructive)·"취소"를 보여준 뒤 signOut한다.

### `RunCanvas/Features/Training/ProgramEditorView.swift`

- **세션을 지울 방법이 없고, 세션 0개 프로그램은 저장됨** — 버그, 70줄
  - 무엇이: `ForEach($draft.sessions) { Section { … } }.onDelete`는 ForEach의 항목이 Section이라 List에 대응하는 행(row)이 없다 — 스와이프 삭제는 행 단위라 이 핸들러는 불리지 않는다(구간의 onDelete만 동작). 결과적으로 만든 세션은 하나도 못 지운다. 반대로 canSave(L39)는 `sessions.allSatisfy`라 세션이 0개면 true → 빈 프로그램이 저장되고 상세 화면엔 '0/0 세션 완료'만 뜬다(구간 0개는 막으면서 세션 0개는 허용해 기준이 어긋남).
  - 어떻게: 세션 Section 안에 `Button("이 세션 삭제", role: .destructive) { draft.sessions.removeAll { $0.id == session.id } }` 한 줄(또는 세션 이름 행에 .swipeActions)을 두고 L70의 onDelete는 지운다. canSave에 `!draft.sessions.isEmpty &&`를 추가.
- **편집 내용을 취소·스와이프로 확인 없이 버린다** — UI/UX, 88줄
  - 무엇이: 24세션짜리 프로그램을 한참 고친 뒤 '취소'를 누르거나 시트를 아래로 밀면 아무 확인 없이 전부 사라진다. 저장 여부 판단 재료(`draft` vs 원본)는 이미 있다.
  - 어떻게: init에서 `original`을 보관하고 `.interactiveDismissDisabled(draft != original)`, 취소 버튼은 변경이 있을 때만 confirmationDialog("변경 내용을 버릴까요?")를 거친다.
- **구간 행: 피커에 접근성 라벨 없음, 큰 글씨에서 시간 라벨이 잘린다** — UI/UX, 102줄
  - 무엇이: `Picker("", …).labelsHidden()`이라 VoiceOver는 "달리기"만 읽고 무엇을 고르는지 알 수 없다. `Stepper { Text(minutesText) }.frame(maxWidth: 160)`은 스테퍼 컨트롤(약 94pt)을 뺀 60여 pt에 "10분 30초"를 넣어야 해서 Dynamic Type 확대 시 "10분…"으로 잘린다.
  - 어떻게: `.accessibilityLabel("구간 종류")` 추가. 행을 `HStack { Picker; Spacer; Text(minutesText); Stepper("", value:…).labelsHidden().fixedSize() }`로 바꿔 시간 텍스트를 스테퍼 프레임 밖으로 뺀다.

### `RunCanvas/Features/Training/TrainingProgramDetailView.swift`

- **내장 프로그램 '복제해서 고치기'가 코드엔 있지만 화면엔 없다** — UI/UX, 55줄
  - 무엇이: ProgramEditorView.init(15-24줄)은 내장 프로그램을 "(내 버전)" 복제본으로 여는 흐름을 갖추고 있지만, 상세 화면은 `!program.isBuiltIn`일 때만 편집 버튼을 보여 줘 8주 5K·30분 도전을 내 몸에 맞게 조정할 방법이 없다. 사용자는 세션 24개를 처음부터 손으로 짜야 한다.
  - 어떻게: `program.isBuiltIn`이면 같은 자리에 `Button("복제해서 수정") { showsEditor = true }`를 두고 기존 편집 시트를 재사용한다(에디터가 이미 복제본을 만든다). 저장 후 목록으로 돌아가면 onAppear reload가 새 프로그램을 보여 준다.
- **내가 만든 프로그램은 세션마다 헤더가 붙어 이름이 두 번 보인다** — UI/UX, 168줄
  - 무엇이: 주차 묶기는 제목의 첫 단어로 키를 만든다. 편집기가 기본으로 주는 "1일차", "2일차"엔 공백이 없어 세션마다 그룹이 하나씩 생기고 `groups.count > 1`이 돼 "1일차 0/1" 헤더 아래 "1일차" 카드가 다시 온다. 세션 5개면 헤더 5개.
  - 어떻게: `let key = session.title.contains(" ") ? firstWord : ""`처럼 공백 없는 제목은 한 그룹으로 모으고, 그룹이 하나면 헤더를 생략한다(이미 `groups.count > 1` 조건이 있어 키만 바꾸면 된다).

### `RunCanvas/Services/AuthService.swift`

- **오프라인 로그아웃 시 이전 사용자의 프로필 캐시가 남음** — 버그, 95줄
  - 무엇이: supabase-swift 2.55.1의 `signOut()`은 로컬 세션을 먼저 지우고(AuthClient.swift:1098-1101) 그 다음 네트워크 로그아웃을 호출하는데, 네트워크 오류는 그대로 throw한다. 그래서 오프라인에서 로그아웃하면 세션은 사라져 로그인 화면으로 가지만 96-97행의 `Profile.clearLocalCache()`는 실행되지 않아, 96행 주석이 막으려던 "다음 사용자에게 이전 계정의 닉네임·아바타·체중이 남는" 상태가 된다. 호출부(SettingsView.swift:264, ProfileSetupView.swift:75)는 `try?`라 사용자에겐 아무 표시도 없다.
  - 어떻게: `signOut()` 첫 줄을 `defer { Profile.clearLocalCache() }`로 바꿔 throw 여부와 무관하게 캐시를 지운다.
- **SDK signOut은 로컬 세션을 지운 뒤 네트워크 오류를 throw → 탈퇴·로그아웃 후처리가 건너뛰어진다** — 버그, 115줄
  - 무엇이: supabase-swift 2.55.1 `signOut(scope:)`(AuthClient.swift:1092-1118)은 `sessionManager.remove()`·`.signedOut` emit을 먼저 하고 그 다음 `/logout`을 호출하며 401/403/404만 삼키고 URLError 등은 그대로 던진다. 따라서 (1) 오프라인 로그아웃(SettingsView:264, ProfileSetupView:75)은 로그인 화면으로는 나가지만 `Profile.clearLocalCache()`(line 97)가 실행되지 않아 다음 사용자가 이전 닉네임·아바타·체중 캐시를 물려받는다(RunCoordinator.weightKg가 이 값을 읽음). (2) `deleteAccount()`에서 RPC 성공 후 `.local` signOut이 일시적 네트워크 오류로 throw하면 `clearLocalCache`·`BadgeStore.reset`·`didDeleteAccount = true`가 빠지고, SettingsView.deleteAccount는 '탈퇴 실패' 알럿을 띄우며 로컬 기록 삭제도 건너뛴다 — 계정은 이미 서버에서 지워졌는데.
  - 어떻게: 두 곳 모두 로컬 정리를 signOut 결과와 분리한다: `deleteAccount`는 `try? await supabase.auth.signOut(scope: .local)`(로컬 제거는 이미 끝났으므로 무시해도 안전), `signOut()`은 `defer { Profile.clearLocalCache() }`로 옮기거나 `try?`로 감싼다.

### `RunCanvas/Services/ChallengeEngine.swift`

- **Calendar.current의 연도를 그대로 키·제목에 씀 — 일본력/불력 기기에서 "0008-08" 키, "2569년 8월" 제목** — 버그, 17줄
  - 무엇이: iOS 설정 > 언어 및 지역 > 캘린더를 일본력·불력으로 두면 `Calendar.current.dateComponents([.year])`가 8(레이와)·2569를 돌려준다. periodKey L17-21은 그 값으로 "0008-08"/"2569-W35" 키를 만들어 UserDefaults에 영구 저장하므로, 캘린더 설정을 바꾸는 순간 지난 완료 챌린지가 전부 "새로 완료"로 다시 잡힌다(RunResultView 라벨 재표시). RecapEngine.title L35-37은 공유용 카드에 "8년 8월"을 찍고, RunStatsView.periodTitle L152도 같은 값을 보여준다. 흔한 설정은 아니지만 저장 키에까지 번지는 문제라 고칠 가치가 있다.
  - 어떻게: 연·월·주 숫자를 뽑는 두 곳(ChallengeEngine.periodKey, RecapEngine.title)에서 `var g = Calendar(identifier: .gregorian); g.timeZone = calendar.timeZone; g.firstWeekday = calendar.firstWeekday`로 만든 그레고리력으로 components를 계산한다(범위 계산은 그대로 두어도 됨). 테스트는 이미 gregorian 캘린더를 넘기므로 그대로 통과한다.

### `RunCanvas/Services/HealthService.swift`

- **건강 오류 문구만 '-습니다' 체 — 앱 전체 '-어요' 톤과 다르고 알럿에 원문 그대로 노출** — 품질, 29줄
  - 무엇이: `HealthError` 문구는 '…사용할 수 없습니다.', '…저장하지 못했습니다.'인데 러닝 화면의 다른 알럿·안내는 전부 해요체다('위치 접근이 꺼져 있어…해 주세요', '기록을 저장할 수 없어요'). RunView:160은 `error.localizedDescription`을 그대로 알럿 본문으로 쓰기 때문에 HealthKit이 던지는 시스템 오류(영문일 수 있음)도 그대로 보이고, 준비해 둔 안심 문구 `"러닝 기록은 계속 사용할 수 있어요."`(RunView:176)는 message가 nil일 때만 쓰여 실제로는 절대 표시되지 않는다.
  - 어떻게: HealthService:29·31을 '이 기기에서는 건강 데이터를 쓸 수 없어요.', '러닝 워크아웃을 건강 앱에 저장하지 못했어요.'로 바꾼다. RunView:160은 `healthAuthorizationMessage = (error as? HealthService.HealthError)?.errorDescription ?? "건강 앱 권한을 확인하지 못했어요."`처럼 우리 문구만 쓰고, 176행 본문은 `Text("\(healthAuthorizationMessage ?? "") 러닝 기록은 계속 사용할 수 있어요.")`로 안심 문구를 실제로 붙인다.

### `RunCanvas/Services/TrainingStore.swift`

- **화면 문구가 저장 키로 쓰이고, 디코딩 실패 시 다음 저장이 기존 프로그램을 전부 지움** — 버그, 17줄
  - 무엇이: TrainingStep.Kind의 rawValue가 "준비 걷기"·"달리기" 같은 화면 문구이고 그대로 JSON에 저장된다(TrainingProgram.swift:7-10). 문구를 하나라도 다듬거나(예: '준비 걷기'→'워밍업') 모델에 필드를 추가하면(합성 Decodable은 기본값이 있어도 키가 없으면 throw) customPrograms가 `try?`로 []를 돌려주고, 그 상태에서 save()가 `[]+새 프로그램`을 write해 사용자가 만든 프로그램이 통째로 사라진다. 지금 코드에서도 UI 문구와 저장 포맷이 한 몸이라 라벨 수정이 곧 데이터 손실이다.
  - 어떻게: Kind rawValue를 "warmup"/"run"/"walk"/"cooldown"으로 바꾸고 `title`에서만 한국어를 돌려준다(이미 title 프로퍼티가 있다). save()는 `defaults.data(forKey:)`가 있는데 디코딩이 실패하면 덮어쓰지 않고 return 하도록 customPrograms를 `[TrainingProgram]?`(nil = 깨짐)로 구분한다.

---

## 이미 고친 것 (77건)

수정자 16명이 자기 파일만 맡아 고쳤다. 빌드·테스트로 확인한 뒤 커밋한다.

- `RunCanvas Watch App/WatchRunView.swift` — 40/41mm 워치에서 조작 버튼이 화면 아래로 밀려 스크롤해야 일시정지할 수 있다 / 종료 버튼이 일시정지 바로 옆 아이콘 하나이고 확인 없이 즉시 러닝을 끝낸다 / 지표 숫자가 빨강/초록으로 칠해져 폰 러닝 화면(흑백 StatLabel)과 톤이 다르다 / 종료 후 화면이 이전 숫자 그대로에 '러닝 시작' 버튼만 돌아와 저장됐는지 알 수 없다 / 1시간 넘는 러닝에서 시간이 '75:00'처럼 표시돼 폰(1:15:00)과 다르다
- `RunCanvas Watch App/WatchWorkoutManager.swift` — start()/end()가 await 중 재진입을 막지 않아 HKWorkoutSession이 두 개 생기거나 .end가 유실된다 / 건강 권한을 거부하면 제목이 'RunCanvas'인 시스템 오류문만 뜨고 어디서 켜야 하는지 안내가 없다 / didFailWithError가 어느 세션이 실패했는지 확인 없이 resetSession()하고, 실패 경로에서 이미 startActivity된 세션을 end() 없이 버린다 / 워치 경과 시간 표시가 폰 스냅샷을 우선해, 폰이 늦게 합류하면 시간이 뒤로 점프한다 / 시작·일시정지·재개·종료에 햅틱이 없어 손목에서 탭이 먹었는지 알 수 없다
- `RunCanvas/Features/Badges/BadgesView.swift` — 레벨 컬러가 LevelCard 진행바·원형 배지로 새어 나감 / 뱃지 획득 표시가 BadgeStore와 어긋남 — 기록 삭제·서버 복원 뱃지가 잠김으로 보임 / 통계 타일이 기록 탭(StatCard)과 뱃지 화면(BestTile)에서 다른 스타일 / BadgeCell 캡션이 28pt 고정 높이라 큰 글씨에서 아래 줄 뱃지와 겹친다 / 날짜 표시가 기기 로케일을 따름 — 영어 설정 기기에서 "08/27/2026 획득", "August", "Aug 27" / 획득 날짜를 onAppear에서 읽어 첫 프레임엔 설명 문구가, 다음 프레임엔 날짜가 보인다 / 장식용 아이콘이 VoiceOver에 읽히고 챌린지 행이 요소 5개로 쪼개진다
- `RunCanvas/Features/Canvas/CanvasStudioView.swift` — 결과 화면·갤러리에서 열면 캔버스가 빈 배경만 나옴 (기본 스티커 없음) / 편집기 상단·인스펙터 버튼 터치 영역이 전부 44pt 미만 / iPhone SE에서 텍스트 스티커 고르면 불투명도 슬라이더가 ~70pt로 찌그러짐 / 경로·심박이 없는 기록에서도 '경로'·'심박수' 스티커를 추가할 수 있어 빈 스티커/"--"가 생김 / 고정 기록으로 열면 기본 스티커가 하나도 안 깔린다 (빈 캔버스) / 텍스트 스티커 30자 제한이 조용히 잘려 사용자가 알 수 없음 / 기록이 고정된 편집기에서 상단 거리 칩이 '비활성 버튼'처럼 흐릿하게 보임 / 설정에서 기록 색을 정해 두면 인스펙터에 '현재 색' 링이 절대 안 뜸 (Color 등호 불일치) / 설정 테마 색은 팔레트 색과 == 가 안 맞아 선택 표시가 안 뜨고 '기본 색' 판정도 깨진다 / VoiceOver에 색 스와치 6개가 전부 "색상"으로 읽히고, 선택된 색·글꼴이 전달되지 않음
- `RunCanvas/Features/Canvas/StickerCanvas.swift` — 경로 스티커가 가로·세로를 따로 늘려 실제 모양과 다르게 찌그러진다 / 회전 0° 스냅이 356°~360° 쪽에서는 안 붙는다
- `RunCanvas/Features/Home/RunnerHomeView.swift` — 날짜를 기기 로케일에 맡김 — 영어 로케일이면 'Sep 6' 로 표시 / 카드 라운드가 12/14/18 로 흩어짐 (규칙 16) / 홈 탭만 바깥 여백 24 — 기록·런꾸·설정 탭은 20 / 홈 탭만 바깥 여백 24pt·카드 라운드 12pt로 다른 탭(20pt·14~16pt)과 어긋남
- `RunCanvas/Features/Marathon/MarathonSchedule.swift` — isAcceptingSignups 가 이미 지난 접수 마감일을 무시함 / 접수 마감일이 지나도 캐시·씨앗의 status 가 open 이면 '접수 중' 으로 보이고 '접수 중' 필터에도 잡힌다 / 5km 필터가 30km·31km·32km·42.195km 종목을 잡음
- `RunCanvas/Features/Marathon/MarathonScheduleView.swift` — 접수 마감된 대회 카드에도 '신청 ›'이 붙는다 / 빈 화면이 '필터가 너무 좁다' 와 '다가오는 일정이 아예 없다' 를 구분하지 않고, 필터 초기화 동작도 없다 / 둘째 필터 줄이 402pt 이하 기기에서 다 안 들어가 — '풀' 칩은 기본 상태에서 안 보이고, 거리 칩을 누르면 지역 알약이 화면 밖으로 밀린다 / 장소가 날짜와 한 줄에 lineLimit(1) 로 붙어 작은 화면·큰 글씨에서 잘리고, 잘린 내용을 볼 곳이 없다 / 지역 메뉴의 개수가 목록에서 숨긴 지난 일정을 포함함 / VoiceOver 가 장식용 심볼(figure.run, chevron.right)을 읽고 'D-13' 을 그대로 읽는다 / 카드의 종목 칩은 'Half'·'Full' 영어인데 바로 위 필터 칩은 '하프'·'풀' 이다
- `RunCanvas/Features/Records/RunStatsView.swift` — 날짜 표기가 기기 로케일을 따라간다 — 영어 기기에서 "September"·"Aug 18"·"08/18/2026 획득" / '최근 러닝' 행이 홈과 기록 탭에서 다른 컴포넌트·다른 라운드 / 월 차트 x축 라벨이 1·6·11·16·21·26·31로 찍힌다 (주석은 5일 간격) / "전체 보기" 링크 터치 영역이 텍스트 크기(약 60×20pt)뿐 / 기록 탭 "최근 러닝" 행 디자인이 홈·전체 기록 목록과 다르다 (RunStatHistoryRow vs RunHistoryRow)
- `RunCanvas/Features/Running/RunDetailView.swift` — 기록 날짜·시각이 기기 로케일을 따름 — 영어 로케일 폰에서 'September 6, 2026 at 5:23 PM' / 기록 날짜가 기기 로케일로 표시됨 — 한국어 고정 규칙 위반 / 기록 상세(24pt 여백·14pt 라운드)와 코스 상세(20pt·16pt)가 나란히 있는데 규격이 다름 / '‘한강 야경런’를 올렸어요' — 코스 이름에 따라 조사가 틀린다
- `RunCanvas/Features/Running/RunSession.swift` — 두 번째 러닝부터 '러닝 시작' 버튼이 무반응 — 세션이 .finished에서 영영 안 돌아옴 / finish 후 state가 .idle로 안 돌아가 앱 재실행 전엔 두 번째 러닝을 시작할 수 없음 / context.save() 실패를 삼키고 체크포인트를 무조건 지워 러닝 기록이 조용히 사라짐 / 벽시계가 뒤로 가면 elapsedSeconds·movingSeconds가 음수로 저장됨
- `RunCanvas/Features/Settings/ProfileEditView.swift` — 큰 키·체중 값 저장 후 프로필 편집 화면이 영구 크래시 / PhotosPicker 필터 `.any(of: [.images, .not(.livePhotos)])`는 사실상 '전부' — 동영상까지 고를 수 있다 / 아바타 카메라 배지가 검정/흰색·systemGroupedBackground 하드코딩 — 다크 모드에서 사라짐 / 프로필 편집은 `.bar`, 프로필 설정은 `.background` — 같은 하단 저장 버튼 인셋의 배경이 다름 / 아바타 카메라 배지가 다크 모드에서 배경에 묻히고 링 색이 화면 배경과 다름 / withTimeout이 Components/Timeout.swift와 중복
- `RunCanvas/Features/Training/TrainingRunView.swift` — 러닝 진행 중에 훈련 세션을 열면 즉시 '훈련 완료'로 찍힌다 / 훈련 끝내기가 조용히 실패하면 뒤로 갈 수 없는 막다른 화면 / 훈련 화면의 일시정지/재개/끝내기가 RunView 와 다른 버튼 스타일 / 러닝 조작 버튼이 RunView와 다르고 '끝내기'가 오탭 위치에 있다
- `RunCanvas/Services/SyncService.swift` — 업로드 중 삭제된 Run에 syncedAt을 써서 크래시 + 서버 행 부활 / 오프라인에서 지운 기록이 다음 동기화 때 되살아난다 / 오프라인 삭제는 서버에 남아 다음 동기화가 기록을 되살린다 / sync 동시 호출이 합쳐지지 않아 같은 기록을 두 번 올리고 두 번 insert한다 / upsert·delete 기본 returning=representation이라 90KB 경로를 서버가 그대로 되돌려준다 / id 목록 조회에 정렬·페이지가 없어 1000건 넘는 사용자는 오래된 기록을 영영 복원 못 한다 / 로컬 fetch 실패를 빈 배열로 삼켜 전 기록을 재삽입하고 런꾸 이미지 링크를 지운다
- `scripts/sync_marathons.py` — 상세 페이지 조회 실패 시 기존 포스터·접수기간·참가비를 NULL로 덮어씀 / 포스터가 한 번 저장되면 접수기간·참가비를 다시 안 읽어 마감일이 영원히 낡음 / 연기·개명·취소된 대회의 옛 행이 날짜가 지날 때까지 DB에 남아 앱에 두 번 나옴 / 씨앗 JSON 의 updatedAt 이 앱에서 안 쓰이는데 매일 바뀌어 develop 에 매일 봇 커밋이 쌓임

---

## 2차 확인 뒤 실제로 남은 것 (26건)

41개 파일을 파일마다 확인한 결과: 이미 닫힘 106 · 문제 아님 15 · 이번에 고침 11 · **남음 26**.
남은 26건은 전부 "확인자가 맡은 파일 밖"이라 미룬 것들이다. 아래 순서로 처리한다.

### A. 동작·데이터 (먼저) (9건)

- [-] 계정 연결 중 스피너·취소 catch의 잔여 문제 (항목 1의 곁가지, 기능 자체는 해결됨) — 다른 파일 필요: RunCanvas/Features/Settings/SettingsView.swift:223-238 — supabase-swift 의 linkIdentity(provider:scopes:redirectTo:) 는 Safari 를 열고 즉시 리턴하므로 link()의 `defer { isLinking = false }` 가 사파리가 뜨자마자 실행되고, `catch let error as ASWebAuthenticationSessionError where error.code == .canceledLogin` 은 구글·카카오 연결 경로에서 도달 불가다(그 catch 는 ASWebAuthenticationSession 을 실제로 쓰는 LoginView 의 signInWithOAuth 경로에만 해당). 연결 자체는 handleOpenURL 이 완료시키므로 결과는 정상이고, 남은 건 버튼이 잠깐만 비활성화된다는 표시상의 문제라 최소 diff 원칙상 이번엔 손대지 않았다.
- [-] AuthService 를 앱 수명으로 한 번만 생성(항목 2의 후반부 제안) — 다른 파일 필요: RunCanvas/App/RunCanvasApp.swift — RunCanvasApp.init 에서 AuthService 를 만들어 `.environment(auth)` 로 내려보내고 AppRouter.swift:6 의 `@State private var auth = AuthService()` 를 `@Environment(AuthService.self)` 로 바꿔야 한다(AppRouter 는 DEBUG 경로에서 `auth.debugUserID` 를 쓰므로 환경 주입 타입 변경까지 동반). 다만 누수 자체는 AuthService 쪽 guard/deinit 으로 이미 막혀 있고, 남는 비용은 RunCanvasApp.body 재평가마다 즉시 해제되는 AuthService 인스턴스 1개(currentSession 읽기 + Task 1개)뿐이라 severity low. 두 파일 시그니처를 동시에 바꾸는 위험 대비 이득이 작다고 판단해 손대지 않았다.
- [x] 워치 쪽 start 신선도 guard 없음 — 다른 파일 필요: RunCanvas Watch App/WatchConnectivityService.swift — receive()의 command 분기 81-83행이 아직 `let timestamp = message["timestamp"] as? TimeInterval ?? 0 / guard timestamp > latestCommandTimestamp else { return } / latestCommandTimestamp = timestamp` 뿐이라 폰이 몇 시간 전에 보낸 .start가 워치 앱을 다음에 열 때 그대로 재생돼 워크아웃이 시작된다. 폰 쪽과 같은 상수 2개(maxStartCommandAge 120 / allowedClockSkew 30)와 isFreshStartCommand 판정을 그 파일에 복사해 `if action == .start` 에만 걸면 된다(워치 타깃은 폰 타깃 코드를 공유하지 않으므로 중복이 최소 diff다).
- [x] 워치 상태 문구 'Watch 단독 기록' — 다른 파일 필요: RunCanvas Watch App/WatchRunView.swift 18행 — `workout.isPhoneReachable ? "iPhone 동기화" : "Watch 단독 기록"` 이 그대로다. 오래된 start를 버리면 워치 단독 러닝은 RunCanvas에 안 남으므로 'iPhone 연결 안 됨 · 건강 앱에만 저장' 같은 문구로 바꿔야 정직하다. 참고로 같은 파일 13행 종료 뒤 문구는 이미 '건강 앱에 저장됐어요'로 되어 있어 톤이 맞다.
- [-] 0거리·0초 Run이 그대로 insert되는 근본 원인 — 다른 파일 필요: RunCanvas/Services/RunSession.swift — 항목 detail이 지목한 finish()의 최소 거리 guard 부재는 이 파일 밖이라 확인만 하고 두었다(내 grep으로는 `func finish` 가 그 경로에서 잡히지 않아 실제 위치·현재 상태는 담당자가 확인해야 한다). start 신선도 guard가 증상을 막긴 하지만, 다른 경로(예: 즉시 시작 후 즉시 종료)로도 0km 기록이 생길 수 있다.
- [x] 왕복 코스에서 화면의 진행도 %가 여전히 튄다 — 다른 파일 필요: RunCanvas/Features/Running/RunView.swift — :62 `updateCourseFollow()`가 아직 전체 검색 API인 `CourseGeometry.progress(at: point, in: course.path)`(:65)를 쓴다. `@State private var lastCourseIndex: Int?`를 추가하고 `CourseGeometry.match(to: point, in: course.path, near: lastCourseIndex)`로 index를 얻어 저장한 뒤 `CourseGeometry.progress(through: index, in: course.path)`로 바꾸면 된다(:66 offCourseMeters는 그대로 두는 게 맞음). CourseGeometry 쪽 API·테스트는 이미 준비돼 있어 지오메트리 파일에서 더 할 일은 없다.
- [x] 건강 오류 알럿에 시스템 원문 노출 + 안심 문구가 절대 안 보임 — 다른 파일 필요: RunCanvas/Features/Running/RunView.swift — 299행이 아직 `healthAuthorizationMessage = error.localizedDescription`이라 HealthKit이 던지는 영문 시스템 오류가 그대로 알럿에 뜬다. `(error as? HealthService.HealthError)?.errorDescription ?? "건강 앱 권한을 확인하지 못했어요."`로 우리 문구만 쓰고, 201행 `Text(healthAuthorizationMessage ?? "러닝 기록은 계속 사용할 수 있어요.")`는 message가 nil일 때만 안심 문구를 쓰므로 두 문장을 이어 붙이도록 바꿔야 한다.
- [-] 다른 파일 필요: RunCanvas/Services/WatchConnectivityService.swift — .start 명령만 reachable일 때 sendMessage로 보내고 errorHandler에서 transferUserInfo로 폴백하기(현재 sendCommand는 전부 transferUserInfo, :44-47). RunCoordinator는 sendCommand를 호출만 하므로 이 파일 안에서는 못 바꾼다. 다만 이 지연이 낳던 실제 피해(워치 심박 중복·HealthKit 워크아웃 두 개, 그리고 이번에 고친 '되쏜 finished로 러닝 종료')는 모두 막힌 상태라, 남은 영향은 '15초 뒤 불필요한 인계 알럿' 정도로 우선순위가 낮다.
- [-] 다른 파일 필요: RunCanvas Watch App/WatchWorkoutManager.swift — 인계 시 폰이 보낸 .end로 워치가 end()→finishWorkout()을 타면(:153-160) 몇 초~몇십 초짜리 짧은 워크아웃이 건강 앱에 남는다. 폰 워크아웃과 나란히 두 개가 보이는 잔여 중복. 폰이 보낸 .end일 때는 discardWorkout로 버리게 하는 편이 맞지만 워치 앱 쪽 변경이라 손대지 않았다.

### B. 날짜·시간대 정확성 (3건)

- [x] 주간 목표 거리 소수 입력이 로케일에 걸려 거부됨 — 다른 파일 필요: RunCanvas/Features/Settings/SettingsView.swift:61 `if let v = Double(text), Self.isValidTarget(v)`. 독일·프랑스 로케일 기기의 decimalPad는 ','만 주는데 `Double("7,5")`는 nil이라 입력이 통째로 무시된다. ProfileMeasurement와 같은 NumberFormatter 파싱으로 바꿔야 한다(상한 `maxTargetKm`은 :312에 이미 있음).
- [x] RecapEngine.title 이 Calendar.current 연도를 그대로 씀 — 다른 파일 필요: RunCanvas/Features/Recap/RecapEngine.swift L34-38 — `calendar.dateComponents([.year, .month], from: anchor)` 를 그대로 써서 일본력 기기에서 공유 카드 제목이 "8년 8월"로 찍힌다. ChallengeEngine L15-19과 같은 방식으로 gregorian 사본을 만들어 components만 뽑으면 된다(집계 구간 dateInterval은 calendar 그대로 둘 것).
- [x] RunStatsView.periodTitle 이 기기 캘린더로 연·월을 포맷 — 다른 파일 필요: RunCanvas/Features/Records/RunStatsView.swift L148-155 — `.locale(ko)` 는 로케일만 바꾸고 FormatStyle의 calendar 는 autoupdatingCurrent 로 남아서, 일본력 기기에서 .year 분기가 "8년"이 된다. `.calendar(Calendar(identifier: .gregorian))` 를 체인에 하나 더 붙이면 해결. 저장 키가 아니라 표시용이라 severity 는 더 낮다.

### C. 터치 영역 44pt (5건)

- [x] 다른 파일 필요: RunCanvas/Components/FilterMenuPill.swift — 알약 히트 영역 44pt (grep으로 `struct FilterMenuPill`이 이 파일에 있음을 확인. 항목의 ':43-46'은 ChipRow.swift가 아니라 이 파일 기준)
- [x] 다른 파일 필요: CourseMapBrowseView.swift — 지도 핀 히트 영역 `.frame(width: 44, height: 44).contentShape(Circle())`
- [x] 다른 파일 필요: CourseDetailView.swift — '이 코스 삭제' 텍스트 버튼 히트 영역 44pt
- [x] 다른 파일 필요: TrainingProgramDetailView.swift — '편집'/'삭제' 텍스트 버튼 히트 영역 44pt
- [x] 다른 파일 필요: TrainingProgramListView.swift — '새로 만들기' 텍스트 버튼 히트 영역 44pt

### D. 톤·간격 통일 (5건)

- [x] 큰 화면 제목 크기 통일(항목 3의 다른 화면 절반) — 다른 파일 필요: RunCanvas/Features/Running/RunResultView.swift L32 `Text("러닝 완료").font(.system(size: 30, weight: .bold))`만 아직 `.largeTitle.bold()`가 아님(Login·ProfileSetup·Splash는 이미 통일됨).
- [x] 바깥 여백 24 → 20(항목 7에 붙은 다른 리뷰어 메모) — 다른 파일 필요: RunResultView.swift L32(.top, 24)·L45·L55(.horizontal, 24), RunCanvas/Features/Auth/LoginView.swift L63, RunCanvas/Features/Settings/ProfileHeader.swift L42 가 아직 24. RunView 안에는 24가 없음.
- [x] Color.card 보조 버튼 통일의 마지막 한 곳 — 다른 파일 필요: RunCanvas/Features/Running/RunDetailView.swift 53~64행 — "이미지로 저장" 버튼이 아직 Label + .font(.headline) + .padding() + Color.card + 라운드 16 을 손으로 조립하고 있다. SecondaryButton(title:systemImage:isLoading:action:) 으로 교체 가능하지만 대상 파일 밖이라 손대지 않았다. 시각 차이는 패딩뿐(현재 .padding() vs 컴포넌트의 minHeight 52 + 가로 16).
- [x] 비활성 스타일 근본 통합 — 다른 파일 필요: RunCanvas/Components/PrimaryButton.swift — PrimaryButtonLabel(현재 20-33행)에 `@Environment(\.isEnabled) private var isEnabled` + `.opacity(isEnabled ? 1 : 0.3)`을 넣으면 호출부 3곳(LoginView, ProfileSetupView.swift:104, ProfileEditView.swift:76)의 수동 .opacity를 지울 수 있다. 다만 PrimaryButtonLabel은 NavigationLink 라벨로도 쓰이므로(RunnerHomeView.swift:80 "러닝 시작") 그쪽 isEnabled 전파를 함께 확인해야 한다.
- [x] 섹션 제목→내용 간격·섹션 사이 간격이 화면마다 다름(다른 화면 쪽 절반) — 다른 파일 필요: RunCanvas/Features/Canvas/CanvasHomeView.swift — L71 `VStack(spacing: 20)` 을 24로. 다른 파일 필요: RunCanvas/Features/Training/TrainingProgramListView.swift — L11 `LazyVStack(alignment: .leading, spacing: 14)`, L47 `VStack(alignment: .leading, spacing: 14)` 를 24/16으로. 다른 파일 필요: RunCanvas/Features/Recap/RecapView.swift — L29 `VStack(spacing: 18)` 을 24로. 리뷰가 언급한 RunStatsView.swift 는 Features/Stats/ 경로에 없어 현재 위치를 확인하지 못했다.

### E. 그 밖 (4건)

- [x] 앱 전체 한국어 로컬라이즈 부재(뒤로 가기 버튼이 제목 폴백일 때 "Back"으로 뜨는 등) — 다른 파일 필요: RunCanvas.xcodeproj/project.pbxproj (293줄 developmentRegion = en, 295줄 knownRegions) — ko를 knownRegions에 넣고 developmentRegion을 ko로 바꾸거나 ko.lproj를 추가해야 한다. RunListView.swift 안에서는 해결 불가. 이번 파일에서 실제로 보이던 영어 문구("Delete")는 위처럼 직접 그려서 없앴다.
- [x] 코스 이름 입력 40자 클램프 — 다른 파일 필요: RunCanvas/Features/Courses/CourseRegisterView.swift — 30줄 TextField에 `.onChange(of: name) { name = String($0.prefix(40)) }` 가 아직 없다. 서버 제약(courses_name_len)이 41자 이상을 거부하므로 데이터는 안전하지만, 사용자는 등록 버튼을 누른 뒤에야 실패를 본다(upload()의 catch가 "등록하지 못했어요"만 띄움). UX 차원의 남은 일이고 스키마 파일에서는 손댈 수 없음.
- [x] 다른 파일 필요: RunCanvas/Features/Running/RunDetailView.swift — 97-101행 `.task(id: run.decoratedImageFilename)` 가 `CanvasStorage.image(filename:)` 결과를 축소 없이 그대로 `decoratedImage` 에 담는다. 3번 항목이 곁다리로 지적한 '`preparingForDisplay()` 가 맞는 자리'가 아직 그대로다(원본 1080×1350 이 상세 화면 하나당 통째로 메모리에 남는다). CanvasHomeView 처럼 detached 블록 안에서 `await ...byPreparingForDisplay()` 로 바꾸면 되지만 대상 파일 밖이라 손대지 않았다.
- [x] 같은 문제가 트레이닝 화면에도 있음 — 다른 파일 필요: RunCanvas/Features/Training/TrainingRunView.swift — 227행 `healthAuthorizationMessage = error.localizedDescription`, 138행 `Text(healthAuthorizationMessage ?? "러닝 기록은 계속 사용할 수 있어요.")`로 RunView와 완전히 동일한 패턴. 리뷰 항목엔 없지만 같은 뿌리라 한 번에 같이 고쳐야 한다.

---

## 3차: 남은 26건 처리 결과 (2026-09-07)

고침 (18)
- 워치 쪽에도 시작 명령 신선도 guard(2분) — 큐에 몇 시간 남아 있던 .start 가 배달돼 워치가 갑자기 워크아웃을 시작하던 것
- 워치 상태 문구 'Watch 단독 기록' → '건강 앱에만 저장' (폰에 안 남는다는 사실대로)
- 건강 권한 실패 알럿에 시스템 영어 원문 대신 우리 문장 (러닝·트레이닝 두 화면)
- 기록 상세의 런꾸 미리보기를 축소해서 읽는다 (1080×1350 원본을 메모리에 들고 있지 않게)
- 코스 이름 40자 클램프
- 따라뛰기 진행도: 직전 지점 주변만 검색 — 왕복 코스에서 반대편으로 튀지 않게
- `Calendar.appGregorian` 도입(그레고리력 + ko_KR, 시간대는 기기 것) → 정산 제목·기록 탭 기간 제목이 불교력 기기에서 '2569년'으로 나오던 것
- 주간 목표 입력이 쉼표 소수점 로케일에서 거부되던 것
- 터치 영역 44pt: 지역 알약 · 지도 핀 · 코스 삭제 · 프로그램 편집/삭제 · 새로 만들기
- 결과 화면 제목 .largeTitle + 여백 20 통일, 로그인·프로필 헤더 여백 24 → 20
- 비활성 표시를 PrimaryButton 이 스스로 하도록(호출부 3곳의 수동 opacity 제거)
- 기록 상세 '이미지로 저장'을 공용 SecondaryButton 으로
- 런꾸 탭 섹션 간격 20 → 24
- 앱 개발 지역을 ko 로 (한국어 전용 앱인데 en 이었다)

안 함 (8) — 이유
- 0거리·0초 Run insert guard: 근본 원인(큐에 남은 start)이 신선도 guard로 막혔다. 지금 버리기를 넣으면
  RunCoordinator.finish 가 '로그인 없음'과 구분이 안 돼 엉뚱한 알럿이 뜬다. 결과 타입을 새로 만들 만한 값은 아직 아니다.
- 워치 .start 를 sendMessage 로 보내고 실패 시 폴백: 신선도 guard 로 피해가 사라졌고, 오류 처리만 늘어난다.
- 인계 시 워치의 짧은 워크아웃이 건강 앱에 남는 문제: 워치 실기기 없이는 확인이 안 된다(다은 몫).
- 계정 연결 스피너·취소 catch: 기능 자체(콜백)는 고쳤다. 남은 건 SDK 가 즉시 반환하는 데서 오는 표시 문제라 후순위.
- AuthService 앱 수명 이동: 현재 `@State private var auth = AuthService()` 라 이미 한 번만 만들어진다(리뷰어 지적은 이론상).
- 그 밖 3건은 확인 결과 이미 닫혀 있었다.

---

## 4차: 리뷰 중 끊겼던 작업 마저 (2026-09-07)

다른 도구로 돌리다 리밋으로 중간에 끊긴 구간이 여러 번 있어, 그 자리에서 남은 것만 다시 훑었다.
확인된 35건(높음 9 · 보통 18 · 낮음 8) 중 34건 처리.

### 동작·데이터

- 코스 등록이 서버 제약을 모른 채 올려 25km 넘는 기록이 400 으로 튕기고 "연결을 확인해 주세요" 만 보이던 것
  → `CourseGeometry.maxPathPoints`(5000) 를 두고 `path(from:)` 이 넘으면 간격을 넓혀 다시 솎는다.
    `CourseService.CourseError.tooLong` 을 추가해 등록 전에 점 수·거리(100km)를 걸러 진짜 이유를 보여준다.
- 워치가 시작(await) 중일 때 온 원격 **일시정지**가 버려져 폰만 멈추고 워치는 계속 달리던 것
  → `pendingEnd` 를 `pendingCommand` 로 넓혀 `.pause` 도 start 끝에서 처리한다.
- 러닝 도중 워치 워크아웃이 실패하면 폰이 30초를 기다린 뒤에야 인계하던 것
  → `didFailWithError` 에서 폰에 `.unavailable` 을 즉시 보낸다.
- 시작 중 폰 세션 ID를 받아들이고도 폰에는 워치의 옛 ID를 되보내 이후 명령이 전부 불일치로 버려지던 것
  → `self.sessionID` 로 보낸다.
- 지난 러닝의 "iPhone 기록으로 전환했어요" 안내가 다음 러닝에서 뒤늦게 뜨던 것 → `start()` 에서 비운다.
- 러닝 화면을 스크롤하는 동안 1초 타이머가 멈춰 시간 표시·음성 안내가 밀리던 것
  → 타이머를 `.common` 런루프 모드에 넣는다.
- 결과 화면을 닫으면 새 러닝이 저 혼자 시작되던 것 → `didAutoStart` 로 한 번만.
- 워치 안내 문구가 러닝 중에도 떠 있던 것 → 시작 전에만.

### 테스트 (없던 자리)

- `SyncServiceTests` 신규 — 서버 삭제 대기 목록의 UserDefaults 왕복·계정 필터·깨진 값 복구.
  (여기가 비면 지운 기록이 다음 다운로드에서 되살아난다)
- `RunCoordinatorTests` 에 반대쪽 경로 2개 — 워치가 워크아웃을 들고 있을 때 심박·종료가 실제로 반영되는지,
  다른 세션의 명령·스냅샷은 무시되는지. (기존 테스트는 "무시한다" 쪽만 봐서 guard 를 넓히면 아무도 안 깨졌다)
- `CourseTests` 에 장거리 솎기 1개.

### 지우기·통일

- 호출부 없는 `CourseService.register(route:)`, `AuthService.isLinked`, `CourseMapView.traveled/isInteractive`,
  `MarathonSchedule.updatedAt` 삭제.
- 스티커 크기 한계가 핀치(0.3~4)와 모서리 손잡이(0.45~3)로 달라 손잡이를 잡는 순간 튀던 것 → 한 범위로.
- `FilterMenuPill` 에 44pt 패치가 두 번 들어가 알약이 부풀어 보이던 것 → 하나로.
- '훈련 끝내기' 탭 영역 44pt (러닝 종료와 같게).
- 섹션 간격 24 통일의 남은 두 곳(트레이닝 목록·정산).
- README 폴더 구조에 코스·마라톤·트레이닝·정산이 빠져 있던 것, 플랜 문서의 서명 설명이 실제와 반대였던 것
  (`Base.xcconfig` 는 커밋 대상, gitignore 되는 건 `Local.xcconfig`) 수정.

### 지도 화면 (데모 코스로 실제 화면을 보고 찾은 것)

- 코스를 고르면 하단 카드가 Apple 지도 저작권 표시(‘지도’ 로고·법적 정보)를 가리고 있었다 — 심사 지적 사항.
  하단 오버레이를 `ZStack` 겹치기에서 `Map.safeAreaInset(edge: .bottom)` 으로 옮겨 MapKit 이 표시를 위로 올리게 했다.
  (데모 코스 2건을 잠깐 넣어 촬영 → 확인 후 삭제. 지금 `courses` 는 다시 0건)

### 안 함 (1) — 판단이 필요

- `TARGETED_DEVICE_FAMILY = "1,2"` + iPad 전 방향 선언: 앱에는 iPad 레이아웃도 가로 레이아웃도 없다.
  iPhone 세로 전용(`"1"` + iPhone 세로만)으로 줄이는 게 맞아 보이지만, 스토어에 어떤 기기로 낼지는
  기능 결함이 아니라 제품 결정이라 손대지 않았다. 바꾼다면 pbxproj 6곳(앱·테스트·UI테스트 × Debug/Release).
