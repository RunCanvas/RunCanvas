# RunCanvas 전체 코드 리뷰 (2026-08-29, develop 599986c)

영역 4개(인증·동기화 / 러닝·워치 / 뱃지·통계·UI / 런꾸)를 독립적으로 리뷰한 결과.
✅ = 실서버·실험으로 재현 확인함, ⚠️ = 코드 읽기로 확인, ❓ = 추정(측정 필요)

---

## 🔴 즉시 (데이터 손실 · 크래시)

### 1. ✅ 업로드 배치가 영구 실패 — 심박 있는 기록 + 없는 기록이 섞이면
`SyncService.swift:43`이 `pending.map(RunDTO.init)`을 한 배열로 upsert. `avgHr`/`maxHr`가 nil이면
Swift가 키를 통째로 생략(테스트 `testNilHeartRateIsOmitted`가 이 동작을 못박아 둠) → PostgREST는
벌크 insert 시 모든 원소의 키 집합이 같아야 함.

**실서버 확인**: 키 다른 2행 → `HTTP 400 PGRST102 "All object keys must match"` (RLS보다 먼저 터짐).
키 같은 2행 → 401(RLS) = 정상 경로.

워치 없이 달린 기록과 워치로 달린 기록이 함께 대기하면 매번 400 → `syncedAt`이 영영 안 찍히고
**폰 분실·재설치 시 그 기록들 복구 불가.**

수정: `RunDTO`에 명시적 `encode(to:)`로 nil도 `container.encode`(=null) 쓰기. 테스트 단언도 뒤집기.
추가로 배치를 건별/청크로 쪼개 한 건 실패가 전체를 막지 않게.

### 2. ✅ HealthKit 저장이 SwiftData 모델을 백그라운드에서 읽음 (PR #28에서 못 고침)
`RunSession.swift:112` `Task { @MainActor in try? await health.saveWorkout(run) }`.
`HealthService.saveWorkout`이 nonisolated async라 SE-0338대로 **호출자 액터를 상속하지 않고 백그라운드로 hop**.
그 안에서 `run.startedAt/distanceMeters/endedAt/calories`를 읽음.

**실험 확인**: `Task { @MainActor }` 진입 = 메인 true → nonisolated async 본문 = 메인 **false**.
112행 주석("run은 SwiftData 모델 — 메인에서만 읽는다")은 사실과 다름. 워치 없는 사용자는 매 러닝마다 이 경로.

수정: finish에서 값 4개를 평범한 struct로 스냅샷해 넘기기(HealthKit 작업이 메인을 막지도 않음).

### 3. ✅ 설정 목표 거리에 큰 수 입력 → 설정 탭 영구 크래시
`SettingsView.swift:266` `String(Int(value))`. 가드는 `v > 0`뿐이라 decimalPad로 1e20 입력 가능 →
UserDefaults에 저장 → 다음 진입 `onAppear`의 `formatted()`에서 트랩.

**실험 확인**: `Fatal error: Double value cannot be converted to Int because the result would be greater than Int.max`
값이 남아 **앱 재실행 후에도 설정 탭 진입 불가.**

수정: 가드에 상한(`v <= 1000`), `formatted`는 `String(format: "%.0f", value)`.

### 4. ⚠️ 위치 권한 거부 처리가 없음 — 0.00km를 저장
`LocationService.authorization`을 **읽는 코드가 코드베이스에 하나도 없음**. 거부/제한 상태에서 러닝 시작하면
타이머만 돌고 거리 0 고정, 경고 없음. 이미 거부됐으면 `requestWhenInUseAuthorization()`이 프롬프트도 안 띄움.
→ 10km 달리고 0.00km 기록 저장.

### 5. ⚠️ 회원 탈퇴: 로컬 먼저 지우고 RPC 나중 → 실패 시 미동기화 기록 영구 소실
`SettingsView.swift:253-257`. 오프라인/서버 오류로 RPC가 실패하면 계정은 살아있는데 로컬 Run은 이미 삭제됨.
서버에 올라간 것은 pull로 복구되지만 `syncedAt == nil`이던 기록(=1번 버그로 못 올라간 것, 오프라인 러닝)은 소실.
수정: `deleteAccount()` 성공 후에만 로컬 삭제.

### 6. ⚠️ 오프라인에서 기존 사용자가 프로필 설정 화면에 갇힘
`AppRouter.swift:80-83` `try?`가 네트워크 오류와 "행 없음"을 구별 못 함 → `hasProfile = false` → ProfileSetupView.
저장도 실패하고, 상단 "로그인으로"를 누르면 signOut되어 오프라인 재로그인 불가 → **앱 완전 잠김.**
러닝 앱에서 오프라인은 일상.

### 7. ⚠️ 진행 중 러닝이 메모리에만 있음 — 앱이 죽으면 전부 소실
`startedAt`·`accumulated`·`route`·거리 어느 것도 디스크에 없고 복구 분기도 없음.
장시간 백그라운드 러닝 중 메모리 압박 종료 시 러닝이 있었다는 사실조차 모름.
수정(최소): 30초마다 JSON 스냅샷 1개, finish에서 삭제, 시작 시 있으면 복구 시트.

### 8. ⚠️ 워치 명령이 조용히 유실 — 종료해도 워치 워크아웃이 안 끝남
`WatchConnectivityService.send()`가 `isReachable` false면 그냥 리턴(폰 `finish()`는 completion도 없음).
워치 화면 꺼져 reachability 끊긴 상태에서 "러닝 종료" → `.end` 유실 → 워치는 `workout-processing`으로 계속 살아
**배터리 마를 때까지 운동이 안 끝나고 건강 앱에 몇 시간짜리 러닝이 남음.**
수정: 명령은 `transferUserInfo`(큐잉·보장 전달), 스냅샷만 `sendMessage`.

### 9. ⚠️ 워치 워크아웃 상태를 백그라운드에서 변경
`WatchWorkoutManager.applyRemoteCommand`의 `Task { await start(...) }`가 MainActor를 상속하지 않는데
`sessionID`(51), `workoutSession/workoutBuilder`(68-69)를 씀. 동시에 메인 타이머가 읽음(155,192).
UUID는 원자적이지 않아 찢어진 값 전송 가능, 옵셔널 참조 동시 접근은 크래시 가능.
수정: 클래스 전체 `@MainActor`.

### 10. ⚠️ 런꾸 "이전" 한 번에 편집 내용 전부 소실
`CanvasFlowView.swift:39` `onBack`이 stickers를 안 돌려줌 → 배경 다시 고르면 기본 4개로 초기화.
수정: `onBack: ([CanvasSticker]) -> Void`.

---

## 🟡 1.0 전에

### 동기화·계정
- **앱 켤 때마다 전 기록의 route를 다시 다운로드** — `SyncService:20` `.select()`에 필터가 user_id뿐 + scenePhase마다 실행.
  route는 1시간 ≈ 90KB → 200건이면 매번 수 MB~십수 MB(셀룰러). 수정: id만 먼저 받아 빠진 것만 2차 조회.
- **뱃지 획득일이 뒤로 밀림** — push가 pull보다 먼저(SyncService:12-13)라, 뱃지 pull이 실패한 뒤 뱃지 화면을 열면
  오늘 날짜로 기록되고 다음 push가 서버의 진짜 획득일을 덮어씀. 수정: `upsert(ignoreDuplicates: true)` + runs/badges do-catch 분리.
- **로그아웃이 프로필 캐시를 안 지움** — `signOut()`에 `Profile.clearLocalCache()` 없음(`deleteAccount`엔 있음).
  `cacheLocally()`가 nil은 건너뛰므로 **A의 체중으로 B의 칼로리가 계산**되고, 홈 헤더에 A의 닉네임·아바타가 남음.
- **계정 전환 시 취소된 task가 이전 계정 프로필을 캐시에 쓸 수 있음**(❓타이밍) — `load()`에 취소 확인 없음.
- **프로필 저장이 서버에서 실패하면 조용히 롤백** — 로컬만 바꾸고 재시도 큐가 없어 다음 실행에 서버 옛 값으로 덮어써짐.
- **`RunDTO.route`가 비옵셔널** — 서버 컬럼은 nullable. null 행 하나면 pull 전체(runs+badges) 실패.
- **날짜 파싱 실패 기록은 `compactMap`으로 조용히 버려짐** — 어느 기기에도 영영 복원 안 됨, 로그도 없음.

### 러닝·워치
- **10초 신선도 필터가 러닝 내내 걸려 백그라운드 거리 손실** — `LocationService:70`. 주석 의도는 "시작 직후 캐시 위치 제외"인데
  실제로는 모든 fix에 적용 → CoreLocation이 배치로 늦게 전달하면 그 구간이 통째로 폐기.
  수정: `lastLocation == nil`일 때만 검사(점프 필터가 이미 이상치를 잡음).
- **워치 명령 핸들러가 RunView가 화면에 있을 때만 동작** — 워치에서 시작한 러닝이 앱에 안 남고, 탭을 옮기면 심박·종료 명령이 버려짐.
  수정: 콜백 등록을 앱 루트로.
- **러닝 중 워치가 끊겨도 폰이 인계 안 함** — `healthManagedExternally`가 시작 시점 한 번으로 고정. 헤더는 "iPhone 기록"인데 실제로는 아무도 기록 안 함.
- **워치 시작 실패가 유실되면 양쪽 다 건강 앱에 저장 안 함** — 워치가 워크아웃 열기 전에 `replyHandler`를 먼저 호출.
  수정: `.start` 후 N초 안에 스냅샷 없으면 자동 `takeOverHealthWorkout()`.
- **양쪽에서 동시에 시작하면 세션 ID가 영구히 갈림** — 이후 모든 명령이 무시됨.
- **세션 만료 시 "러닝 종료"가 무반응** — `RunView:141` guard가 조용히 return, 뒤로가기도 막혀 있어 기록 전체 소실.
- **종료 시 "일시정지"를 먼저 읽음** — `finish()`가 정산용으로 `pause()`를 부르는데 거기서 음성이 나감.
- **평균 심박이 편향** — 값이 바뀔 때만 샘플링해서 변동 큰 구간 쪽으로 끌림.
- **HealthKit 쓰기 거부가 조용히 무시** — `requestAuthorization`은 쓰기 거부해도 throw 안 하고, `saveWorkout` 실패는 `try?`가 삼킴.
- **스냅샷 전송 과다** — 워치가 `updateStatistics` 루프 안에서 타입마다 전송(초당 3~5회). 양쪽 배터리 직접 영향.
- **`RouteMapView.segments`가 body마다 전 경로 재계산** — 마라톤이면 렌더마다 CLLocation 1만 6천 개 할당.

### 뱃지·통계·UI
- **뱃지 화면이 렌더 1회에 전체 배열을 ~50번 재매핑, 스트릭 정렬 ~78회** — 기록 1,000건이면 체감 멈춤.
  수정: body 진입 전 지역 상수로 한 번만 계산. `RunStatsView`도 같은 문제(13번 재매핑).
- **❓ 앱 루트 `@Query`가 합계 하나 때문에 전 기록의 route 블롭을 메모리에 올림** — 500건 ≈ 45MB.
  RootTabView·Home·Stats·Badges 4중. Instruments 측정 필요.
- **첫 러닝 뱃지 캡션이 "0.00/0 km"** — category가 `.distance`인데 target은 1(횟수).
- **마라톤 캡션 "42.00/42 km"인데 잠김** — `Int(42195/1000)` = 42 절삭. 하프도 21.00km에서 동일.
- **블랙 레벨이 다크 모드에서 안 보임** — `Theme`에 `accent/onAccent` 가드를 만들어 뒀는데 `LevelCard`만 원색 사용.
  진행바는 채워진 구간이 안 보여 0%처럼 보임.
- **홈 헤더 기본 닉네임이 "다은"** — UI 테스트 스크린샷에 그대로 박힘.
- **`dismissKeyboardOnTap`이 실제로 히트테스트 안 됨** — `.contentShape(Rectangle())` 누락.
- **`Keyboard`·톤 리터럴** — RunStatsView 4곳이 `Color.gray.opacity(0.08)` 리터럴, 홈 2곳은 0.1이라 미세하게 진함.

### 런꾸
- **`loadTransferable` 타임아웃 없음** — 같은 리포 `ProfileEditView`에 20초 타임아웃 헬퍼와 경고 주석이 이미 있는데 재사용 안 함.
  진행 표시도 없어 앱이 멈춘 것처럼 보이고 실패해도 조용함.
- **원본 해상도 UIImage를 플로우 내내 유지** — 48MP ≈ 195MB. 최종 산출물이 1080×1350이라 화질 이득 0.
- **`saveToPhotos()`가 nonisolated async인데 @State를 씀** — 바로 위 `render()`엔 `@MainActor`가 붙어 있음. 권한 거부 시 백그라운드에서 SwiftUI 상태 변경.
- **기록·계정을 지워도 `Documents/canvas/*.jpg`가 영구 잔존** — `CanvasStorage`에 delete가 없음. iCloud 백업에도 포함.
- **렌더 실패 시 무한 스피너** — `ImageRenderer.uiImage`가 Optional인데 nil 처리 없음, 빠져나갈 길은 "이전"뿐.
- **크기 조절이 비선형·기기마다 다름** — 리사이즈만 local 좌표계 + `/100` 상수가 캔버스 폭과 무관.
- **정렬 가이드가 그려지기만 하고 스냅 없음** — 매 프레임 부모 @State 갱신으로 전체 재렌더.
- **앱에서 유일한 빨강 UI** — 아무것도 삭제하지 않는 버튼에 `role: .destructive` + 빨강 4중.
- **권한 문구가 규칙과 어긋남** — 플랜은 "문자열 키는 pbxproj", PR은 Info.plist에 중복 기록.
- **❓ 편집 화면과 내보낸 이미지의 글자 크기가 다를 수 있음** — Dynamic Type이 `ImageRenderer`엔 안 걸림.

---

## 🟢 후속 / 사소
- 탈퇴 시 아바타 삭제가 `try?` — 공개 버킷이라 실패하면 얼굴 사진이 URL 아는 사람에게 계속 노출.
- `runs`에 `updated_at`/삭제 마커 없음 — 기록 삭제 UI가 생기는 순간 pull이 삭제한 기록을 되살림. (지금 RunListView에 스와이프 삭제가 이미 있음 → **실제로 재현될 수 있음**)
- `SyncService`가 `print`로만 에러를 남김(릴리즈 포함), 재시도·백오프 없음.
- `ownerID ?? UUID()` — 렌더마다 새 predicate, BadgesView는 무작위 키로 UserDefaults에 쓰기까지.
- `BadgeStore.completedChallenges`가 write-only(앱에서 안 읽음), `Challenge.detail` 미사용.
- `ChallengeRow` 클램프 불일치 — "5 / 3회", "최장 12.30 km / 10 km".
- 접근성: ProgressView 라벨 없음, BadgeCell이 개별로 읽힘, 잠김 상태가 시각으로만 전달, chevron이 읽힘.
- 테스트 공백: `firstWeekday = 1`(실제 ko_KR 기본) 미검증, 말일/연말주/윤년, 백그라운드 배치 fix, 워치 프로토콜 전무.
- 죽은 코드: `Features/Running/RunningView.swift`(참조 0, 첫 UI 때 플레이스홀더).
- deprecated: `.alert(item:)`+`Alert`, `.coordinateSpace(name:)`, `static var defaultValue`(Swift 6에서 에러).
- 런꾸 미완성: 뱃지 스티커(렌더 코드는 있는데 추가 메뉴 없음), 핀치 확대, 가이드 스냅, 저장본 삭제·교체 경로.
- 프리뷰 2개에 `.modelContainer` 없음(BadgesView).

---

## 잘 된 점 (구조적으로 옳게 잡힌 것)
1. **순수 함수 엔진 분리** — `RunMath`/`BadgeEngine`/`ChallengeEngine`/`StatsEngine`이 SwiftData와 완전 분리 + `Calendar` 주입.
   덕분에 타임존 고정 테스트가 결정적으로 돌고, `BadgeRun` 3필드 struct 하나를 세 엔진이 공유.
2. **GPS 점프 필터가 스파이크에 앵커링되지 않음** — 튄 좌표가 기준점이 되어 이후 거리가 통째로 망가지는
   가장 흔한 버그를 구조적으로 회피. 테스트가 정확히 그 성질을 검증.
3. **시간을 타이머 틱이 아니라 날짜 차이로 누적** — 백그라운드에서 타이머가 씹혀도 경과 시간이 안 어긋남.
   resume 시 `lastLocation = nil`로 일시정지 중 이동거리 유입도 차단.
4. **런꾸 좌표계 설계** — 위치 0~1 정규화 + 크기를 캔버스 폭 비례로. 편집 캔버스와 렌더가 같은 `StickerCanvas`라
   "미리보기와 결과가 다른" 이 기능 최대의 함정을 회피.
5. **계정 필터를 한 번도 안 빠뜨림** — 모든 `@Query`가 ownerID predicate. 런꾸 신규 코드도 동일 패턴 준수.
6. **실서버 응답을 테스트 픽스처로 박아둔 것** — Postgres 가변 소수점 날짜 4종을 커버.
7. **`delete_own_account`가 security definer + search_path + auth.uid() 체크 + revoke 4종 세트**.
8. **파일 경로를 상대 경로로 저장** — 앱 업데이트마다 컨테이너 절대경로가 바뀌는 iOS의 전형적 실수 회피.
