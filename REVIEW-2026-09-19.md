# 전체 리뷰 — 출시 직전 (2026-09-19)

`develop` 기준. 앱 코드 55개 파일 11,384줄 + `docs/supabase/schema.sql`을 네 영역으로 나눠 전부 읽었다.

## 처리 현황

발견한 항목은 영역별 PR 5개로 나눠 고쳤다. 하나의 큰 PR로 묶지 않은 이유는 출시 직전에
55개 파일을 한 번에 바꾸면 리뷰가 불가능하고 문제가 생겼을 때 되돌릴 단위가 없기 때문이다.

| PR | 범위 | 이 문서의 항목 |
|---|---|---|
| #59 | 워치·동기화 데이터 손실 | 블로커 1·3·4, 높음 5·6·7, 0초 guard |
| #60 | 보안 (라이브 DB 적용 완료) | 블로커 2, 높음 8·9, nonce·owner 필터 |
| #61 | 런꾸 편집·저장 | 높음 10·11, 보통 런꾸 7건 |
| #62 | 화면 접근성·로케일·터치 영역 | 높음 12, 보통 화면 4건, 낮음 다수 |
| #63 | 배터리·렌더 지연·디자인 시스템 | 낮음 성능·이탈 9건 |

**머지 순서:** #62를 #63보다 먼저. #62에 실기기용 테스트 수정(`XCTSkipUnless`)이 들어 있어,
먼저 들어가면 #63의 실기기 테스트 실패 2건이 사라진다.

**고치지 않은 것**
- `[-]` 표시 항목 — 각 항목에 판단 근거를 적어 뒀다
- 런꾸 편집기 크롬의 본문 고정 폰트 13곳 — 출시 직전에 조밀한 툴바의 폰트를 Dynamic Type 으로
  바꾸는 건 레이아웃 회귀 위험이 실익보다 크다고 봤다. 출시 후 정리 대상

**직접 확인이 필요한 것** — 코드로는 확인했지만 손으로 만져봐야 아는 것들
- #61 스티커 손잡이·제스처 (작은 스티커의 X 버튼이 눌리는지)
- #62 접근성 레이아웃 (큰 글씨에서 뱃지 "최고 기록")
- 맨 아래 "미확인" 절의 3건

| 영역 | 범위 |
|---|---|
| 러닝 코어·워치 | RunSession, RunCoordinator, RunView, Location/Health/WatchConnectivity, 워치 앱 전체 |
| 데이터·동기화·인증 | Models 전부, SyncService, AuthService, AppRouter, schema.sql |
| 화면 | 홈·기록·설정·코스·마라톤·트레이닝·뱃지·리캡 |
| 런꾸·컴포넌트 | Canvas 전체, Components 전체, 디자인 시스템 준수 |

**표기:** `[x]` 고쳐야 함 · `[-]` 확인했으나 고치지 않기로 함
**✅ 직접 확인** 표시가 붙은 항목은 리뷰 종합 단계에서 원본 코드를 다시 열어 재확인했다. 나머지는 영역 담당의 보고를 그대로 옮긴 것이다.

이전 리뷰 `REVIEW-2026-09-07.md`의 26건 대조 결과는 맨 아래에 있다.

---

## 🔴 출시 블로커 (4건)

### 1. `[x]` 달리는 중인 워치 러닝을 폰이 삼킨다 — "8km를 3초에" ✅ 직접 확인

`RunCanvas Watch App/WatchWorkoutManager.swift:454-463`

워치가 이미 달리는 중인데 폰에서 러닝을 시작하면, 워치가 **조건 없이** 폰의 세션 ID로 갈아탄다.

```swift
case .start:
    guard state == .idle || state == .finished, !isStarting else {
        // 양쪽에서 거의 동시에 시작한 경우 — 폰을 마스터로 보고 세션 ID를 맞춘다.
        self.sessionID = sessionID     // ← 50분째 달리는 세션도 그냥 갈아탄다
        return
    }
```

폰 쪽에는 대칭 방어가 있다(`RunCoordinator.swift:129-135` — "내가 달리는 중이면 내 ID를 알려준다"). **워치 쪽에만 "내가 얼마나 달렸는지"를 보는 조건이 없다.**

ID가 맞춰지는 순간 워치 스냅샷이 폰의 `apply()`를 통과하고 `RunSession.swift:283`의 `distanceBase`가 첫 값에서 기준을 잡는다 → 폰 러닝 3초 만에 8.00km. 이어서 `finish(sendToWatch: true)`가 `.end`를 보내 **워치의 진짜 러닝을 강제 종료**한다.

**재현:** 손목에서 시작해 달리는 중 폰을 꺼내 "러닝 시작". 폰 앱이 그 사이 꺼져 있었으면 폰은 워치 러닝을 모른다.

**수정:** `elapsedSeconds > 10`이면 동시 시작이 아니라 이미 달리는 중이므로 `.unavailable`을 돌려보내 폰이 자기 세션을 접게 한다.

---

### 2. `[x]` avatars 버킷 SELECT 정책이 익명 전수 열거를 허용한다 ✅ 직접 확인

`docs/supabase/schema.sql:46-47`

```sql
create policy "avatar read" on storage.objects
  for select using (bucket_id = 'avatars');
```

`to` 절이 없어 기본값이 `public`(= `anon` 포함)이고, 조건이 `bucket_id`뿐이다. Storage의 `list()`가 이 SELECT 정책을 그대로 탄다.

저장소가 공개로 전환됐으므로 `sb_publishable_` 키는 이미 공개다. **로그인 없이** 다음이 가능하다.

1. `POST /storage/v1/object/list/avatars` → 폴더명 = **전 사용자 auth uid 목록**
2. 각 uid로 공개 URL → **전 사용자 프로필 사진 일괄 수집**

"아바타 공개"의 의도는 *URL을 아는 사람이 한 장을 본다*였는데, 실제 정책은 *아무나 사용자 명부와 얼굴 사진을 통째로 긁는다*이다.

**수정:** 공개 버킷의 표시 경로(`/object/public/...`)는 **RLS를 타지 않으므로** 이 정책은 표시에 불필요하다. 열거만 열어 주고 있다.

```sql
drop policy "avatar read" on storage.objects;
create policy "avatar read own" on storage.objects
  for select to authenticated
  using (bucket_id = 'avatars' and (select auth.uid())::text = (storage.foldername(name))[1]);
```

---

### 3. `[x]` 경로를 나중에 채운 기록의 서버 사본이 경로 없이 영구 고정 ✅ 직접 확인

`RunCanvas/Services/HealthImport.swift:144-148`

`syncedAt`은 **되돌리는 코드가 저장소 어디에도 없다** — 대입 3곳(`RunDTO.swift:75`, `SyncService.swift:139`)이 전부 "찍기"만 한다.

```swift
for run in runs where run.route.isEmpty {
    run.route = workout.route      // 이미 업로드된 기록을 고치는데 syncedAt은 그대로
    filledRoutes += 1
}
```

`pushPending`은 `syncedAt == nil`만 올린다. 워치 러닝 → 경로 빈 채로 저장 → 업로드·`syncedAt` 찍힘 → 나중에 경로 채움 → **영영 재업로드 안 됨.**

기기 교체·재설치로 복원하면 워치로 뛴 기록은 전부 지도가 비어 있고, 런꾸 경로 스티커를 못 쓴다. 복원된 기록은 `healthWorkoutID`가 nil이라(낮음 항목) **건강 앱에서 다시 채울 수도 없다.**

**수정:** `run.route = workout.route` 아래에 `run.syncedAt = nil` 한 줄.

---

### 4. `[x]` `importMissingRuns`가 직렬화되지 않아 같은 워크아웃이 기록 2건이 된다

`RunCanvas/App/AppRouter.swift:47-55`, `RunCanvas/Services/HealthImport.swift:112-153`

`SyncService.sync()`는 `syncTail`로 재진입을 막지만, **그 앞에 붙은** `importMissingRuns`에는 가드가 없다. 로컬 스냅샷을 먼저 읽고(L118) 그 뒤 긴 await(L132, 90일치 + 경로 쿼리)가 온다. 두 번째 호출이 그 사이에 들어오면 같은 스냅샷을 보고 중복 판정을 둘 다 통과한다.

`makeRun`이 `Run(id: UUID())`이라 **id가 다른 두 행**이 생겨 `@Attribute(.unique)`가 막지 못하고, `pushPending`이 둘 다 올린다.

**재현:** `.active → .inactive → .active`(알림 배너, 앱 스위처, 제어 센터). 가장 위험한 건 **설치 직후 첫 실행** — 90일치를 가져오느라 쿼리가 가장 길다.

**수정:** `AppRouter`에 `@State private var syncTask`를 두고 진행 중이면 건너뛴다.

---

## 🟠 높음 (8건)

| # | 항목 | 파일 |
|---|---|---|
| 5 `[x]` | 워치가 **폰이 준 거리**로 `discardWorkout()`을 결정한다. 블루투스가 끊겨 폰이 20m만 쟀으면 워치의 실제 5km를 날린다 → `max(finalDistance, distanceMeters)` 한 줄 | `WatchWorkoutManager.swift:252,262` |
| 6 `[x]` | 세션 만료로 `auth.userID`가 nil이 되면 `ownerID`가 지워져 진행 중 러닝을 저장 못 한다. 워치 종료 경로는 **아무 표시 없이** 실패 → setter에서 nil 무시 | `RunCoordinator.swift:24` |
| 7 `[x]` | 워치 알럿에 영문 HealthKit 오류가 샌다(3곳). 폰은 이미 고쳤다 | `WatchWorkoutManager.swift:212,305,501` |
| 8 `[x]` | `courses.owner_id` 공개 + `<uid>/avatar.jpg` 고정 경로 = 코스 올린 사람 얼굴이 계산으로 도출된다. #2를 고쳐도 남는다 ✅ 직접 확인 | `schema.sql:127`, `ProfileService.swift:23` |
| 9 `[x]` | avatars 버킷에 용량·MIME 제한이 없다. 로그인 한 번이면 `text/html`을 올려 프로젝트 도메인에서 피싱 페이지를 서빙할 수 있다 | `schema.sql:45` |
| 10 `[x]` | 스티커 손잡이가 `scaleEffect`를 같이 타 기본 배치 22pt, 작게 줄이면 **9pt**. 캔버스 위에서 삭제가 불가능하다 | `StickerCanvas.swift:210-226` |
| 11 `[x]` | 두 손가락 조작 한 번이 실행취소 스택에 2~3칸을 쌓는다. 되돌리기가 고장난 것처럼 보인다 | `StickerCanvas.swift:230,241,264,281` |
| 12 `[x]` | 코스·마라톤 세 화면이 첫 프레임에 "아직 없어요"를 잘못 띄운다(`isLoading` 초기값 false + `.task`가 첫 렌더 뒤 실행) → 서비스 초기값을 `true`로 | `CourseListView:106`, `CourseMapBrowseView:52`, `MarathonScheduleView:110` |

---

## 🟡 보통 (17건)

**동작·데이터**
- `[x]` 0초 러닝이 저장된다 — `finish()`에 시간 하한이 없다. 이전 리뷰 A-5의 "0거리·0초" 중 **거리만 닫혔다** (`RunSession.swift:141`)
- `[x]` `end()` 실패 시 워크아웃이 finish도 discard도 안 된 채 버려진다 (`WatchWorkoutManager.swift:277-307`)
- `[x]` `ProfileSetupView` 재진입이 기존 아바타를 `NULL`로 덮어쓴다 (`ProfileSetupView.swift:141`)
- `[x]` `AppRouter`가 `sync()`의 직렬화 꼬리를 우회해 `pullMissing`을 직접 부른다 (`AppRouter.swift:116`)
- `[x]` `try! ModelContainer` — 스토어가 안 열리면 앱이 영구 실행 불가. 다음 모델 변경 때 터진다 (`RunCanvasApp.swift:12`)
- `[-]` schema.sql의 storage 정책 4개는 **라이브 DB와 대조된 적이 없다.** 이 저장소는 드리프트 전력이 있다 → 출시 전 `pg_policies` 조회 필요

**런꾸**
- `[x]` 배경 종류를 바꿔 재저장하면 확장자가 `png`↔`jpg`로 갈려 **이전 파일이 영구 고아** (기록당 2~4MB) (`CanvasStorage.swift:17`)
- `[x]` 공유가 `UIImage`를 그대로 넘겨 투명 배경이 사라진다. 사진 저장 경로는 이 문제 때문에 데이터로 직접 넣는데 공유만 빠졌다 (`CanvasExportSheet.swift:62`)
- `[x]` 여러 개 선택이 스티커를 건드리는 순간 무너진다 (`StickerCanvas.swift:42`)
- `[x]` 롱프레스와 드래그가 같은 10pt 경계를 공유해 선택·회전 모드가 껐다 켜진다 (`StickerCanvas.swift:227-230`)
- `[x]` `context.save()` 실패 시 모델 필드를 안 되돌려 DB·디스크가 어긋난다. 삭제 경로는 이미 복구하는데 저장만 빠졌다 (`CanvasExportSheet.swift:142`)
- `[x]` 공유만 하고 나가면 "꾸미던 내용을 버릴까요?"가 뜬다 (`CanvasExportSheet.swift:41`)
- `[x]` 꾸미던 중 기록을 바꾸면 이전 기록 기준의 경로·심박 스티커가 빈 스티커로 남는다 (`CanvasStudioView.swift:438`)

**화면**
- `[x]` 접근성 글자 크기에서 "최고 기록" 3열 카드 값이 잘린다. 같은 파일 뱃지 그리드는 2열로 내리는데 여기만 안 한다 (`BadgesView.swift:114-127`)
- `[x]` 뱃지 획득 날짜·기록 상세 날짜의 **연도**가 기기 달력에 끌려간다(불교력 "2569년"). `Calendar.appGregorian`이 이미 있는데 두 곳만 빠졌다 (`BadgesView.swift:253`, `RunDetailView.swift:52`)
- `[x]` 터치 영역 44pt 누락 4곳 — "복제해서 수정"(`TrainingProgramDetailView.swift:72`), 설정의 "연결"·"해제"(`SettingsView.swift:209,217`), 오류 카드의 "다시 시도"(코스 2곳·마라톤 1곳)
- `[x]` 챌린지 기간 계산이 `Calendar.current`라 저장 키(그레고리)와 어긋난다. `periodKey`는 변환하는데 `periodRange`만 안 한다 (`ChallengeEngine.swift:30-34`)

---

## ⚪ 낮음 (요약)

**보안·정합성**
- `[x]` `AppleNonce.random`이 `SecRandomCopyBytes` 반환값을 버린다. 실패 시 nonce가 `"000…0"` 고정이 되고 재전송 방지가 사라진다 ✅ 직접 확인 (`AppleSignInButton.swift:81`)
- `[x]` `pullRuns` 본문 조회에 `user_id` 필터 없음 / `CourseService.delete`에 `owner_id` 필터 없음 — 현재는 RLS가 막지만 이중 방어가 빠졌다
- `[x]` 탈퇴 시 `TrainingStore` 캐시와 `pendingDeletes`가 남는다
- `[x]` `RunDTO`에 `healthWorkoutID`가 없어 복원된 기록은 경로를 되찾을 수 없다 (#3과 겹침)

**표시·일관성**
- `[x]` `.monospacedDigit()` 누락 — 기록 탭 40pt 히어로 숫자 포함 11곳
- `[x]` 영문 시스템 오류 노출 3곳 (`RunListView:82`, `RecapView:123`, `RunDetailView:123`)
- `[x]` 숫자 포맷이 기기 로케일을 타 한 줄에 구분자가 섞인다 (`MarathonScheduleView:380`, `ProfileHeader:63`)
- `[x]` 레벨 카드가 `tier.color/foreground`를 직접 써 블랙 레벨이 다크 모드에서 묻힌다. 진행바에도 레벨 색을 칠한다 (`BadgesView.swift:139,144,157`)
- `[x]` 훈련 중 `finish()`가 false면 나갈 길이 없다 — 알럿 "확인"이 아무것도 안 한다 (`TrainingRunView.swift:125`)

**디자인 시스템 이탈**
- `[x]` 규정 외 라운드 4·9·10·18 (런꾸 6곳, 리캡 1곳)
- `[x]` 앱 톤 시트의 그림자 3곳
- `[x]` 편집기 크롬의 본문 고정 폰트 13곳 (문서의 "현재 편차" 목록에 이 파일이 빠져 있다)

**성능·기타**
- `[x]` 앱 수명 내내 3초마다 깨는 루프, 취소 지점 없음 (`RunCoordinator.swift:213`)
- `[x]` 워치가 초당 3~5건 메시지를 보낸다(타이머 + HK 콜백 이중) (`WatchWorkoutManager.swift:533`)
- `[x]` 정산·런꾸 렌더가 메인 스레드를 막아 스피너가 안 돈다
- `[-]` 뱃지 화면이 뱃지마다 전체 기록을 다시 훑는다(O(19×N)). 현재 데이터량에선 체감 안 됨
- `[-]` `HealthService` 심박 쿼리의 강한 참조 순환 — 앱 수명 객체라 실사용 영향 작음
- `[-]` `latestCommandTimestamp` 단조 필터가 시계 역행에 취약 — 실기기 시계 오차는 미확인
- `[-]` `StatsEngine`의 강제 언래핑 3곳 — iOS가 주는 달력에서는 nil이 안 나온다

---

## 확인했고 문제 없음

- **거리 산수·걸음 보정** — `distanceBase` ↔ `watchDistanceMeters` ↔ GPS 전환을 양방향으로 전개. 거리가 뒤로 점프하거나 겹친 구간이 두 번 쌓이는 조합 없음
- **워크아웃 중복 저장(정상 경로)** — `healthManagedExternally` + `HealthImport.overlaps`(±120초) 이중 판정으로 막혀 있다
- **RLS 핵심 3테이블** — `profiles`/`runs`/`user_badges` 전부 `auth.uid()` 본인 한정. `delete_own_account()`도 `security definer` + 본인 한정 + cascade
- **업로드 배치 부분 실패** — PostgREST 벌크 upsert는 단일 트랜잭션. 중복 행이 생기지 않는다
- **계정 전환 격리** — `ownerID` 필터, 계정별 키, `AppRouter`의 늦은 응답 가드 3곳 모두 정확
- **메인 액터 안전성·참조 순환** — WatchConnectivity 델리게이트 6개 전부 메인으로 넘기고, 클로저는 `[weak self]`. 크래시 조합 못 찾음
- **0으로 나누기·인덱스 범위** — `RunMath`·`StatsEngine`·`ChallengeEngine`·`RecapEngine` 전부 가드 있음
- **권한 처리** — 사진·카메라·위치·건강 전부 거부 분기 + 설정 링크. Info.plist 문구가 Debug/Release 양쪽에 있다
- **팔레트 hex 20여 개가 디자인 문서와 정확히 일치.** 새 색 0건, 이모지 0건, 규정 외 모션 0건
- **VoiceOver** — 아이콘 전용 컨트롤에 라벨이 전부 있고, 장식 아이콘은 `accessibilityHidden(true)`

---

## 이전 리뷰(2026-09-07) 26건 대조

**A. 동작·데이터 (9건)** — 고쳐짐 4 · 절반 2 · 열림 1 · 담당 밖 2

| 항목 | 상태 |
|---|---|
| 워치 `.start` 신선도 guard | **고쳐짐** |
| 워치 문구 "Watch 단독 기록" | **고쳐짐** |
| 왕복 코스 진행도 튐 | **고쳐짐** |
| `.start` sendMessage 폴백 | **고쳐짐**(다른 방식) |
| 0거리·0초 Run | **절반** — 거리만 닫힘, 0초는 열림 |
| 건강 알럿 원문 노출 + 안심 문구 | **절반** — 원문은 닫힘, 두 문장 이어 붙이기는 열림 |
| 인계 시 워치가 짧은 워크아웃 저장 | **열림** — `.end`에 `discard` 플래그가 필요 |
| 계정 연결 스피너 / `AuthService` 수명 | 담당 밖 — `AppRouter`에 `@State auth` 그대로 |

**B. 날짜·시간대 (3건)** — 전부 닫힘. 단 같은 규칙이 뱃지·기록 상세에는 안 따라붙었다(위 보통)

**C. 터치 영역 (5건)** — 전부 닫힘. 단 같이 추가된 새 버튼 4곳에 안 따라붙었다(위 보통)

**D. 톤·간격 (5건)** — 4 닫힘 · 1 사소(`TrainingProgramListView:47` 간격 14)

**E. 그 밖 (4건)** — 전부 닫힘

**런꾸 18건** — 17 닫힘 · 1 부분(기록 교체 경로)

---

## 미확인 — 출시 전 확인 필요

1. **라이브 DB의 storage 정책** — 이 리뷰의 #2·#8·#9 판정은 전부 `schema.sql` 문면 기준이다. 실제 적용 여부를 조회해야 한다.
   ```sql
   select policyname, cmd, roles, qual from pg_policies where schemaname='storage';
   select id, public, file_size_limit, allowed_mime_types from storage.buckets where id='avatars';
   ```
2. **홈 히어로 카드 고정 높이 276** — AX5에서 250~265pt로 계산되나 폰트 메트릭 가정이 섞여 있다. 실기기 확인 필요
3. **저장 성공 알럿에서 시트와 cover를 같은 틱에 닫는 패턴** — iOS 18/26에서 실제로 깨지는지 미확인

---

## 권장 순서

1. **블로커 4건** — #1·#3은 각각 몇 줄, #2는 SQL 한 번, #4는 가드 하나
2. **워치 데이터 손실 2건** (#5·#6) — 워치 실기기 검증 **전에** 고쳐야 원인 구분이 된다
3. **보안 나머지** (#8·#9 + 낮음의 nonce)
4. **런꾸 손맛 3건** (#10·#11 + 다중 선택) — 핵심 차별 기능의 첫인상
5. 나머지는 출시 후 정리 가능
